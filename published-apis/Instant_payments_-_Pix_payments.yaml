openapi: 3.1.0
info:
  title: Instant payments - Pix payments
  description: API to handle Pix transactions
  contact:
    name: API Support
    url: https://developers.pismo.io/support/
  license:
    name: Copyright Pismo
  version: '1.0'
servers:
  - url: https://sandbox-cdn.pismolabs.io
    description: Sandbox API server for testing
  - url: https://sandbox-cdn.pismolabs.io/inst-pull-pay
    description: Sandbox API server for testing instant pull pay.
tags:
  - name: Pix
  - name: Scheduled transfers V2
  - name: Scheduled transfers
  - name: Withdrawal services
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  parameters:
    AccountIDpath:
      name: account_id
      in: path
      schema:
        type: integer
      description: Pismo account ID.
      required: true
      example: 5869300
    AfterIdQuery:
      in: query
      name: afterId
      description: >-
        Last returned item ID from response. Use this field to return more
        results.
      required: false
      schema:
        type: string
    AuthHeader:
      in: header
      name: Authorization
      required: true
      description: >-
        Account token - an access token encoded with a Pismo account ID. Tokens
        can expire quickly, which can result in a <b>401 Unauthorized</b>
        message.
      schema:
        type: string
        example: Bearer eyJhbGci...IUzUx
    BeginDateQuery:
      name: beginDate
      in: query
      description: >
        Begin date filter. RFC 3339 format. For example:
        `2023-04-12T23:20:50.52Z`. 


        Maximum days allowed between `beginDate` and `endDate` is 30.
      required: true
      schema:
        type: string
      example: '2023-04-12T23:20:50.52Z'
    BeginDateQuery2:
      name: beginDate
      in: query
      description: >
        Begin creation date for scheduler executions filter. Format =
        yyyy-mm-dd.
      schema:
        type: string
      example: '2023-04-12'
    CIDheader:
      name: x-cid
      description: >
        The Correlation ID UUID field links related API requests and events. The
        `x-cid` can help the Pismo engineering team track everything related to
        a call. If not passed, this field is automatically generated.
      in: header
      required: false
      schema:
        type: string
    EndDateQuery:
      name: endDate
      in: query
      description: >
        End date filter. RFC 3339 format. For example:
        `2023-04-12T23:20:50.52Z`. 


        Maximum days allowed between `beginDate` and `endDate` is 30.
      required: true
      schema:
        type: string
      example: '2023-04-12T23:20:50.52Z'
    EndDateQuery2:
      name: endDate
      in: query
      description: |
        End creation date for scheduler executions filter. Format = yyyy-mm-dd.
      schema:
        type: string
      example: '2023-04-12'
    EndToEndIDpath:
      name: end_to_end_id
      in: path
      description: >-
        Transaction end-to-end ID. This field is a Brazil Central Bank (BCB)
        requirement to track all Pix transaction steps.
      required: true
      schema:
        type: string
    EndToEndIDquery:
      name: endToEndId
      in: query
      schema:
        type: string
      description: >
        Transaction end-to-end ID filter. This field is a Brazil Central Bank
        (BCB) requirement to track all Pix transaction steps.
      required: true
    ExecutionIDpath:
      name: execution_id
      in: path
      description: Sequential number incremented for each scheduled transfer execution.
      required: true
      schema:
        type: string
    ExecutionStatusQuery:
      name: status
      in: query
      required: false
      schema:
        type: string
        enum:
          - PENDING
          - SUCCESS
          - FAIL
          - CANCELLED
          - PROCESSING
          - DELIVERED
      description: Execution status filter
      example: PENDING
    IdempotencyKeyHeader:
      name: Idempotency-Key
      in: header
      description: >
        A unique ID to ensure the operation remains idempotent, allowing for
        operation repitition without causing unintended effects or duplication.
        An idempotent operation is one that can be applied multiple times, yet
        the outcome remains the same. it ensures that network errors, retries,
        or failures can occur without introducing inconsistencies.
      required: true
      schema:
        type: string
    ISPBquery:
      in: query
      name: ispb
      schema:
        type: string
        maxLength: 8
      description: >-
        <a
        href="https://bank.codes/numero-do-banco/bank/banco-central-do-brasil/"
        target="_blank">ISPB (Identifcador do Sistema de Pagamento Brazil)</a> -
        the Brazilian Payment System ID, an 8-digit value.
      required: true
    ISPBtransmitterQuery:
      in: query
      name: ispbTransmitter
      schema:
        type: string
        maxLength: 8
      description: >-
        <a
        href="https://bank.codes/numero-do-banco/bank/banco-central-do-brasil/"
        target="_blank">ISPB (Identifcador do Sistema de Pagamento Brazil)</a> -
        the Brazilian Payment System ID, an 8-digit value. The ISPB transmitter
        ensures the payment request is directed to the correct receiving
        institution based on the ISPB code associated with the beneficiary's
        account.
      required: true
    lastEvaluatedCreationDatetimeQuery:
      name: lastEvaluatedCreationDatetime
      in: query
      description: >-
        This is the `last_evaluated_creation_datetime` field in the returned
        result and is used to get the next page of results in a subsequent
        request.

        RFC 3339 format. For example -  "2023-04-12T23:20:50.52Z". 
      schema:
        type: string
    limitQuery:
      in: query
      name: limit
      description: Maximum number of agents to return. Default is all.
      schema:
        type: integer
      example: 20
    OrderQuery:
      name: order
      in: query
      description: >
        Order to return items: `ASC` = ascending order, `DESC` = descending.
        Default is `DESC`.
      schema:
        type: string
        enum:
          - ASC
          - DESC
    PageQuery:
      name: page
      in: query
      description: Page number. Default = `1`.
      required: true
      schema:
        type: string
      example: '1'
    PerPageQuery:
      name: perPage
      in: query
      description: Per page amount (min=1, max=100). Default is `20`.
      required: true
      schema:
        type: string
    ReversalEndToEndIDquery:
      name: reversalEndToEndId
      in: query
      description: Reversal end-to-end ID filter.
      schema:
        type: string
    ScheduleBeginDatetimeQuery:
      name: scheduleBeginDatetime
      in: query
      description: >
        Schedule datetime begin filter. RFC 3339 value, for example:
        `2023-04-12T23:20:50.52Z`. 
      schema:
        type: string
    ScheduleEndDatetimeQuery:
      name: scheduleEndDatetime
      in: query
      description: >
        Schedule datetime end filter. RFC 3339 value, for example:
        `2023-04-12T23:20:50.52Z`. 
      schema:
        type: string
    ScheduleCreationBeginDatetimeQuery:
      name: scheduleCreationBeginDatetime
      in: query
      description: >
        Schedule creation datetime begin filter. RFC 3339 value, for example:
        `2023-04-12T23:20:50.52Z`. 
      schema:
        type: string
    ScheduleCreationEndDatetimeQuery:
      name: scheduleCreationEndDatetime
      in: query
      description: >
        Schedule creation datetime end filter. RFC 3339 value, for example:
        `2023-04-12T23:20:50.52Z`. 
      schema:
        type: string
    ScheduleIdPath:
      name: scheduler_id
      in: path
      description: Schedule ID
      required: true
      schema:
        type: string
        maxLength: 36
      example: '8675309'
    ScheduleIdQuery:
      name: schedulerId
      in: query
      description: Schedule ID filter
      required: false
      schema:
        type: string
        maxLength: 36
      example: '8675309'
    SchedulerIntervalQuery:
      in: query
      name: interval
      required: false
      schema:
        type: string
        enum:
          - DAY
          - WEEK
          - MONTH
      description: Scheduler interval filter
      example: DAY
    SchedulerStatusQuery:
      name: status
      in: query
      schema:
        type: string
        enum:
          - ACTIVE
          - INACTIVE
          - CANCELLED
          - COMPLETED
      description: Scheduler status filter
      required: false
      example: ACTIVE
    SchedulerTypeQuery:
      name: type
      in: query
      required: false
      schema:
        type: string
        enum:
          - ONCE
          - RECURRING
      description: Scheduler type filter
      example: ONCE
    ShowPreviewQuery:
      name: showPreview
      in: query
      required: false
      schema:
        type: boolean
      description: >-
        Should a preview of upcoming executions be presented? Default is `false`
        (no execution previews returned).
      example: true
    TransactionTypePath:
      name: type
      in: path
      description: Pix transaction type
      schema:
        type: string
        enum:
          - PIX-IN
          - PIX-OUT
          - PIX-IN-REVERSAL
          - PIX-OUT-REVERSAL
      required: true
      example: PIX-IN
    TransactionTypeQuery:
      name: type
      in: query
      description: Pix transaction type filter
      schema:
        type: string
        enum:
          - PIX_IN
          - PIX_OUT
          - PIX_IN_REVERSAL
          - PIX_OUT_REVERSAL
      example: PIX_IN
  schemas:
    AccountID:
      type: integer
      description: Pismo account ID.
      example: 5869300
    AccountNumber:
      type: string
      description: Account number.
      maxLength: 20
      example: '98765432'
    AccountNumber2:
      type: string
      description: Payer's account number.
      maxLength: 20
      example: '98765432'
    AccountType:
      type: string
      enum:
        - CACC
        - SLRY
        - SVGS
        - TRAN
      maxLength: 4
      description: |
        Account type:  <br />
          `CACC` - Checking/current account.  Known as "conta corrente" in Portuguese, this is a bank account type designed for everyday financial transactions and is the most common type individuals and businesses use for day-to-day banking.<br />
          `SVGS` - Savings account<br />
          `TRAN` - Payment account. Payment institutions offer payment accounts to move funds and make payments, while banks offer checking accounts that include full financial services such as credit and investments.<br />
          `SLRY` - Salary account. Employer deposits a fixed amount every month.<br />
      example: TRAN
    AdditionalInformation:
      type: string
      description: Additional information
      maxLength: 105
    AgentModality:
      type: string
      description: >
        Withdrawal service agent. **REQUIRED** for `CHANGE` and `WITHDRAWAL`
        transactions.<br />
          * `AGTEC` - Agente estabelecimento comercial. For commercial establishment.<br />
          * `AGFSS` - Agente Facilitador de Serviço de Saque. For cash machine.<br />
          * `AGTOT` - Agente Outra Espécie de Pessoa Jurídicas ou correspondente no País. Withdrawal service facilitator.<br />
      enum:
        - AGTEC
        - AGTOT
        - AGPSS
      example: AGTEC
    AllowsValueChange:
      type: boolean
      description: Can the payer change the final amount?
      example: false
    Amount:
      type: number
      description: Amount to be transferred.
      example: 1050.44
    AuthorizationID:
      type: integer
      description: Generated authorization ID on P2P
      example: 82725717
    Bank:
      type: string
      description: >-
        <a
        href="https://bank.codes/numero-do-banco/bank/banco-central-do-brasil/"
        target="_blank">ISPB (Identifcador do Sistema de Pagamento Brazil)</a> -
        the Brazilian Payment System ID for the receiving bank, an 8-digit
        value.
      maxLength: 8
      example: '237'
    BeneficiaryID:
      type: string
      maxLength: 36
      description: >
        Beneficiary ID. This ID created when the [Register
        beneficiary](ref:post-intl-bank-accounts-v1-bank-accounts-beneficiaries)
        endpoint is called.
      example: 97c96d96-9b87-404b-88e5-287f2b0e6e0a
    Branch:
      type: string
      description: This could be a bank-assigned branch ID or the branch's ISPB number.
      maxLength: 4
      example: '011'
    CityCode:
      type: string
      description: >-
        The <a href='https://www.ibge.gov.br/explica/codigos-dos-municipios.php'
        target="_blank">IBGE</a> (Instituto Brasileiro de Geografia e
        Estatística) city code.
      example: '3550308'
    ClientRequestID:
      description: |
        For Pix-out, this is the client-generated ID used for idempotency.
        For Pix-in, this is the confirmation request ID the provider received.
      type: string
      example: b84da2fc-dd63-4c6c-a346-797b65f334c7
    commons.AdditionalInfo:
      type: object
      properties:
        name:
          type: string
          description: QR Code name information.
          example: Discount
        value:
          type: string
          description: QR Code value information.
          example: Paying until the 5th, there will be a 10% discount
    commons.Charges:
      type: object
      properties:
        discount_value:
          $ref: '#/components/schemas/DiscountValue'
        interest_value:
          $ref: '#/components/schemas/InterestValue'
        fine_value:
          $ref: '#/components/schemas/FineValue'
        settled_value:
          $ref: '#/components/schemas/SettledValue'
    commons.Dates:
      type: object
      properties:
        creation_datetime:
          type: string
          description: UTC creation date time.
          example: '2020-09-15T19:39:54.013Z'
        due_date:
          type: string
          description: Due date, format = yyyy-mm-dd
          example: '2020-09-15'
        expiration_datetime:
          type: string
          description: UTC expiration date time.
          example: '2020-09-15T19:39:54.013Z'
        presentation_datetime:
          type: string
          description: UTC presentation date time.
          example: '2020-09-15T19:39:54.013Z'
        validity_after_expiration_day:
          type: integer
          description: >-
            How long, in days, the QR Code is active or functional after the
            expiration date.
          example: 30
    commons.Debtor:
      type: object
      properties:
        document_number:
          $ref: '#/components/schemas/DocumentNumber'
        name:
          $ref: '#/components/schemas/Name'
    commons.Payee:
      type: object
      properties:
        type:
          $ref: '#/components/schemas/PersonType'
        account_number:
          $ref: '#/components/schemas/AccountNumber'
        account_type:
          $ref: '#/components/schemas/AccountType'
        address:
          type: string
          description: Payee street address.
          example: 1542 3rd Ave
        bank:
          $ref: '#/components/schemas/Bank'
        branch:
          $ref: '#/components/schemas/Branch'
        city:
          type: string
          description: Payee city.
          example: New York
        document_number:
          $ref: '#/components/schemas/DocumentNumber'
        key:
          $ref: '#/components/schemas/Key'
        name:
          $ref: '#/components/schemas/Name'
        participant_id:
          type: integer
          description: Pix participant ID (ISPB).
          example: 60701190
        state:
          type: string
          description: Payee state.
          example: NY
        zip_code:
          $ref: '#/components/schemas/PostalCode'
    ContractID:
      type: string
      description: |
        Payee-created external ID for their reference.
      maxLength: 35
      example: 5509-uroo-78944-9987
    CreatedAt:
      type: string
      format: datetime
      description: |
        Created at datetime. RFC 3339 format 
      example: '2025-11-14T17:54:40Z'
    customer.DetailMessage:
      type: object
      properties:
        external:
          type: array
          items:
            $ref: '#/components/schemas/customer.ExternalMessage'
        parameters:
          type: array
          items:
            $ref: '#/components/schemas/customer.ParameterMessage'
        payload:
          type: array
          items:
            $ref: '#/components/schemas/customer.PayloadMessage'
    customer.Error:
      type: object
      properties:
        code:
          type: string
          description: Error's code.
          example: EPT0001
        details:
          $ref: '#/components/schemas/customer.DetailMessage'
        message:
          type: string
          description: Error's message.
          example: User not found
    customer.ExternalMessage:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: string
          description: Error code.
          example: PBE315
        message:
          type: string
          description: Error message.
          example: Generic error
    customer.ParameterMessage:
      type: object
      required:
        - field
        - message
      properties:
        field:
          type: string
          description: Parameter field
          example: Query parameter
        message:
          type: string
          description: Message about parameter
          example: Generic error
    customer.PayloadMessage:
      type: object
      required:
        - field
        - message
      properties:
        field:
          type: string
          description: Payload parameter
          example: Entity.field
        message:
          type: string
          description: Message about error
          example: Generic error
    Debtor:
      type: object
      required:
        - document_number
        - document_type
        - name
      properties:
        document_number:
          $ref: '#/components/schemas/DocumentNumber'
        document_type:
          $ref: '#/components/schemas/PersonType'
        name:
          $ref: '#/components/schemas/Name'
    DiscountValue:
      type: number
      description: Discount value.
      example: 0.15
    DocumentNumber:
      type: string
      description: >
        A government document number, such as a Social Security number (US) or
        Cadastro de Pessoas Físicas number (Brazil). Must be numeric with 11-14
        digits.
      minLength: 1
      maxLength: 14
      example: '123412341234'
    EndToEndID:
      type: string
      description: >
        This field is a Brazil Central Bank (BCB) requirement to track all steps
        of Pix transactions. 

        Basically, there are two steps to a Pix transaction:
            1. Initialize a transaction 
            2. Confirm a transaction
        The `end_to_end_id` is sent to the BCB in both steps in order to
        identify all transaction information. 
      example: E3030629420200808195101608910248
    ExecutionID:
      type: integer
      description: Sequential number incremented for each scheduled transfer execution.
      example: 2
    ExecutionStatus:
      type: string
      description: Execution status
      enum:
        - PENDING
        - SUCCESS
        - FAIL
        - CANCELLED
        - PROCESSING
        - DELIVERED
      example: PENDING
    FineValue:
      type: number
      description: Fine value.
      example: 5.15
    FirstPaymentDate:
      type: string
      format: datetime
      description: First charge/payment date. Format = yyyy-mm-dd
      example: '2025-04-25'
    FlexControls:
      type: object
      description: >
        [Flex control
        ID](https://developers.pismo.io/pismo-docs/docs/flex-controls#pix-and-accounts)
        and schedule limit checking before creating a new scheduler.
      required:
        - pix_limit_id
      properties:
        pix_limit_id:
          type: string
          description: Rule ID for scheduled Pix limit
          example: 279207b0-d553-424e-a454-8d704cca3702
    FrequencyType:
      type: string
      description: Recurrence payment type
      enum:
        - WEEK
        - MONTH
        - QUARTER
        - SEMESTER
        - YEAR
      example: MONTH
    HistoryItem:
      type: object
      properties:
        status:
          $ref: '#/components/schemas/RecurrenceStatus'
        updated_at:
          $ref: '#/components/schemas/UpdatedAt'
    IdempotencyKey:
      type: string
      description: Idempotency key
      example: d2c3f4e5-6a7b-8c9d-e0f1-2g3h4i5j6k7l
    InitialAmount:
      type: string
      description: The initial purchase price amount before charges or withdrawal changes.
      example: '10.02'
    InitiationType:
      type: string
      enum:
        - MANUAL
        - DICT
        - STATIC_QRCODE
        - DYNAMIC_QRCODE
        - PAYMENT_INITIATOR
        - AUTOMATIC_PIX
        - DYNAMIC_PROXIMITY_PIX
        - STATIC_PROXIMITY_PIX
      maxLength: 20
      description: |
        Initiation type:  

          `MANUAL` - Manually initiated. Key is not used - bank account, bank code, document number, and branch are used for identification.

          `DICT` - Diretorio de Identificadores de Contas de Transacapo (Transaction accounts ID directory). Pix/DICT key is used for identification.

          `STATIC_QRCODE` - Reusable code that contains only the data necessary to complete a transaction. When using static QR Code, the transaction amount must be provided manually.

          `DYNAMIC_QRCODE` - The amount and other details for a dynamic code are included automatically. 
          
          `PAYMENT_INITIATOR` - Payment provider initiated. A payment initiator is a 3rd party that has obtained authorization from the Central Bank and has an <a href="https://www.bcb.gov.br/en/financialstability/open_finance" target="_blank">Open Finance</a> certification.

          `AUTOMATIC_PIX` - Payment order initiated via automatic PIX.
          
          `DYNAMIC_PROXIMITY_PIX` - Dynamic Near Field Communications (NFC) based initiation.

          `STATIC_PROXIMITY_PIX` - Static NFC-based initiation.
      example: DICT
    ScheduleInitiationType:
      type: string
      enum:
        - MANUAL
        - DICT
        - STATIC_QRCODE
        - DYNAMIC_QRCODE
      maxLength: 20
      description: |
        Initiation type:  

          `MANUAL` - Manually initiated. Key is not used - bank account, bank code, document number, and branch are used for identification.

          `DICT` - Diretorio de Identificadores de Contas de Transação (Transaction accounts ID directory). Pix/DICT key is used for identification.

          `DYNAMIC_QRCODE` - The amount and other details for a dynamic code are included automatically. 
      example: DICT
    InitiationType2:
      type: string
      enum:
        - MANUAL
        - DICT
        - STATIC_QRCODE
        - DYNAMIC_QRCODE
      maxLength: 20
      description: |
        Initiation type:  

          `MANUAL` - Manually initiated. Key is not used - bank account, bank code, document number, and branch are used for identification.

          `DICT` - Diretorio de Identificadores de Contas de Transação (Transaction accounts ID directory). Pix/DICT key is used for identification.

          `DYNAMIC_QRCODE` - The amount and other details for a dynamic code are included automatically. 
          
      example: DICT
    InterestValue:
      type: number
      description: Interest value.
      example: 20.15
    Internal:
      type: boolean
      description: >-
        Was transaction made between a Pismo organization's accounts? In this
        case, the instant payment system is not called.
      example: false
    Interval:
      type: string
      description: Scheduler interval
      enum:
        - DAY
        - WEEK
        - MONTH
    IsDeviceRegistered:
      type: boolean
      description: Is device registered to do a Pix-out transfer? Default is `true`.
      example: true
    ISPB:
      type: string
      description: >-
        <a
        href="https://bank.codes/numero-do-banco/bank/banco-central-do-brasil/"
        target="_blank">ISPB (Identifcador do Sistema de Pagamento Brazil)</a> -
        the Brazilian Payment System ID, an 8-digit value.
      maxLength: 8
      example: '60701190'
    JourneyType:
      type: string
      description: |
        Recurrence payment type: 
         `AUT1` - Bank notification. If the merchant has the payer's banking information, a push notification can be sent directly to the payer's bank app, prompting them to authorize the recurring payment.
         
         `AUT2` - Authorization via QR Code. Payer scans a merchant provided QR Code to authorize recurring payments. This method does not initiate an immediate payment, allowing the first payment to be scheduled for a later date.
         
         `AUT3` - Authorization via QR Code with first payment.  Similar to the second method, but the QR Code includes information for both the authorization and the initial payment, enabling the payer to authorize the recurring payment and make the first payment in a single step.
         
         `AUT4` - Post-payment sign-up path. After completing a one-time payment via QR Code, the payer is offered the option to authorize recurring payments for future transactions

         For more information, refer to [Pix automatic use cases (journeys)](doc:pix-automatic-beta-copy-2#pix-automatic-use-cases-journeys).
      enum:
        - AUT1
        - AUT2
        - AUT3
        - AUT4
      example: AUT1
    Key:
      maxLength: 77
      type: string
      description: Pix key value.
      example: email@email.com
    LastID:
      type: integer
      description: |
        Last returned item ID. Use this ID to get more results.
      example: 1602619
    LastPaymentDate:
      type: string
      format: datetime
      description: Last charge/payment date. Format = yyyy-mm-dd
      example: '2026-06-10'
    Metadata:
      type: object
      description: >
        Key-value fields for adding custom information to request. Total maximum
        object size = 3000 bytes.


        **Note**: This field must not be used to send Personally Identifiable
        Information (PII), Payment Card Industry (PCI) data, or any
        sensitive/regulated information. Metadata fields are intended for
        operational, non-sensitive data only. For sensitive data, use the
        specific parameters designed for that  purpose. For more information,
        refer to [Get started with Pismo
        APIs](ref:get-started-with-pismo-apis#metadata).
    MinPaymentAmount:
      type: number
      description: Minimum payment amount for dynamic amount recurrences, defined by payee.
      example: 10.09
    Name:
      type: string
      description: Name
      maxLength: 200
      example: Walter Melon
    NextPage:
      type: boolean
      description: Are tbere more results?
      example: false
    NumberElements:
      type: number
      description: Total items returned in query
    PageSize:
      type: number
      description: Maximum elements returned per query.
    Payee:
      type: object
      required:
        - document_number
        - document_type
        - name
        - bank
        - branch
        - account_number
        - account_type
      properties:
        name:
          $ref: '#/components/schemas/Name'
        document_number:
          $ref: '#/components/schemas/DocumentNumber'
        document_type:
          $ref: '#/components/schemas/PersonType'
        bank:
          $ref: '#/components/schemas/Bank'
        branch:
          $ref: '#/components/schemas/Branch'
        account_number:
          $ref: '#/components/schemas/AccountNumber2'
        account_type:
          $ref: '#/components/schemas/AccountType'
    Payer:
      type: object
      required:
        - document_number
        - document_type
        - name
        - bank
        - branch
        - account_number
      properties:
        document_number:
          $ref: '#/components/schemas/DocumentNumber'
        document_type:
          $ref: '#/components/schemas/PersonType'
        name:
          $ref: '#/components/schemas/Name'
        bank:
          $ref: '#/components/schemas/Bank'
        branch:
          $ref: '#/components/schemas/Branch'
        account_number:
          $ref: '#/components/schemas/AccountNumber2'
        account_type:
          $ref: '#/components/schemas/AccountType'
        city_code:
          $ref: '#/components/schemas/CityCode'
    PaymentType:
      type: string
      description: |
        Payment type.
        * `IMMEDIATE` - Immediate payment. 
        * `FRAUD` - Payment due to suspected fraud.
        * `SCHEDULED` - Scheduled payment.
      enum:
        - IMMEDIATE
        - FRAUD
        - SCHEDULED
      example: IMMEDIATE
    PerPage:
      type: integer
      description: Max number of items per page.
      example: 10
    PersonType:
      type: string
      description: |
        Person type:  
        * `LEGAL_PERSON` - An actual person with legal rights.
        * `NATURAL_PERSON` - An entity, such as an organization or company.
      enum:
        - LEGAL_PERSON
        - NATURAL_PERSON
      example: NATURAL_PERSON
    pix.GetTransactionByTypeAndE2EResponse:
      type: object
      properties:
        amount:
          $ref: '#/components/schemas/Amount'
        end_to_end_id:
          $ref: '#/components/schemas/EndToEndID'
        payee:
          $ref: '#/components/schemas/pix.TransactionExternalAccount'
        payer:
          $ref: '#/components/schemas/pix.TransactionExternalAccount'
        status:
          $ref: '#/components/schemas/TransactionStatus'
    pix.InReversalRequest:
      type: object
      required:
        - account_id
        - amount
        - end_to_end_id
        - reason
      properties:
        account_id:
          $ref: '#/components/schemas/AccountID'
        amount:
          $ref: '#/components/schemas/Amount'
        end_to_end_id:
          type: string
          description: >
            This field is a Brazil Central Bank (BCB) requirement to track all
            steps of Pix transactions. <br />

            Basically, there are two steps to a Pix transaction:<br />

            1. Initialize a transaction <br />

            2. Confirm a transaction<br />


            The `end_to_end_id` is sent to the BCB in both steps in order to
            identify all transaction information.<br />


            On reversal flow, the `end_to_end_id` is the original transaction
            `end_to_end_id`.<br />
          example: E3030629420200808195101608910248
        reason:
          $ref: '#/components/schemas/ReversalReason'
        additional_information:
          $ref: '#/components/schemas/AdditionalInformation'
        description:
          type: string
          description: Reversal description
          maxLength: 140
    pix.InReversalResponse:
      type: object
      properties:
        end_to_end_id:
          $ref: '#/components/schemas/EndToEndID'
        reversal_end_to_end_id:
          type: string
          description: >
            This field is a Brazil Central Bank (BCB) requirement to track all
            steps of Pix transactions. 

            Basically, there are two steps to a Pix transaction:

            1. Initialize a transaction 

            2. Confirm a transaction

            The `end_to_end_id` is sent to the BCB in both steps in order to
            identify all transaction information.


            Here, the `reversal_end_to_end_id` is generated automatically and
            begins with a "D".
        reversal_status:
          type: string
          description: Reversal status
          enum:
            - SETTLED
            - PROCESSING
            - ERROR
          example: SETTLED
    pix.Meta:
      type: object
      properties:
        date_time_data_generation:
          type: string
          description: Date/time date generated. RFC 3339 format.
          example: '2023-04-12T23:20:50.52Z'
        ispb_transmitting:
          type: string
          description: ISPB (Brazilian bank ID) transmitting
          example: '87653099'
        name_transmitting:
          type: string
          description: Name of ISPB transmitting
          example: Joe's bank
        total_pages:
          type: string
          description: Total pages
          example: '10'
        total_records:
          type: string
          description: Total records
          example: '20'
    pix.OutTransferCollectSignatureRequest:
      type: object
      required:
        - account_id
        - scheduler_id
        - execution_id
        - end_to_end_id
        - psp_transaction_created_at
        - signature_info
      properties:
        account_id:
          $ref: '#/components/schemas/AccountID'
        scheduler_id:
          $ref: '#/components/schemas/SchedulerID'
        execution_id:
          $ref: '#/components/schemas/ExecutionID'
        idempotency_key:
          $ref: '#/components/schemas/IdempotencyKey'
        end_to_end_id:
          $ref: '#/components/schemas/EndToEndID'
        recurrence_id:
          $ref: '#/components/schemas/RecurrenceID'
        psp_transaction_created_at:
          $ref: '#/components/schemas/PSPtransactionCreatedAt'
        signature_info:
          $ref: '#/components/schemas/pix.SignatureInfo'
    pix.OutTransferCollectSignatureResponse:
      type: object
      required:
        - scheduler_id
        - execution_id
        - end_to_end_id
        - status
      properties:
        scheduler_id:
          $ref: '#/components/schemas/SchedulerID'
        execution_id:
          $ref: '#/components/schemas/ExecutionID'
        end_to_end_id:
          $ref: '#/components/schemas/EndToEndID'
        status:
          type: string
          description: Current transaction status
          enum:
            - PROCESSING
          example: PROCESSING
    pix.OutTransferRequest:
      type: object
      required:
        - account_id
        - amount
        - bank_account
        - initiation_type
        - ispb
        - payee
        - transaction_type
      properties:
        account_id:
          $ref: '#/components/schemas/AccountID'
        amount:
          $ref: '#/components/schemas/Amount'
        bank_account:
          type: object
          description: Payer's bank account details.
          required:
            - account_number
            - account_type
            - branch
          properties:
            account_number:
              $ref: '#/components/schemas/AccountNumber2'
            account_type:
              $ref: '#/components/schemas/AccountType'
            branch:
              $ref: '#/components/schemas/Branch'
        initiation_type:
          $ref: '#/components/schemas/InitiationType'
        ispb:
          $ref: '#/components/schemas/ISPB'
        payee:
          type: object
          description: Payee's information. The payee is the transfer receipient.
          required:
            - account_number
            - account_type
            - bank
            - branch
            - document_number
            - name
          properties:
            account_number:
              $ref: '#/components/schemas/AccountNumber'
            account_type:
              $ref: '#/components/schemas/AccountType'
            bank:
              $ref: '#/components/schemas/Bank'
            branch:
              $ref: '#/components/schemas/Branch'
            document_number:
              $ref: '#/components/schemas/DocumentNumber'
            name:
              $ref: '#/components/schemas/Name'
            beneficiary_id:
              $ref: '#/components/schemas/BeneficiaryID'
            key:
              $ref: '#/components/schemas/Key'
            type:
              $ref: '#/components/schemas/PersonType'
        transaction_type:
          $ref: '#/components/schemas/TransactionType'
        charges:
          type: object
          description: Charges details. **REQUIRED** if returned from QR Code validation.
          properties:
            discount_value:
              $ref: '#/components/schemas/DiscountValue'
            interest_value:
              $ref: '#/components/schemas/InterestValue'
            fine_value:
              $ref: '#/components/schemas/FineValue'
            settled_value:
              type: number
              description: >-
                Settlement value. Amount to be transferred after all charges
                have been applied.
              example: 10.15
        debtor:
          type: object
          description: >-
            Debtor information. The debtor could be the payer, or someone who
            made a purchase on the payer's behalf.
          properties:
            document_number:
              $ref: '#/components/schemas/DocumentNumber'
            name:
              $ref: '#/components/schemas/Name'
        end_to_end_id:
          type: string
          description: >
            This field is a Brazil Central Bank (BCB) requirement to track all
            steps of Pix transactions. 

            Basically, there are two steps to a Pix transaction:
              1. Initialize a transaction 
              2. Confirm a transaction

            The `end_to_end_id` is sent to the BCB in both steps in order to
            identify all transaction information.

            **REQUIRED** if you are using a Pix key or QR Code. 
          example: E3030629420200808195101608910248
        is_device_registered:
          $ref: '#/components/schemas/IsDeviceRegistered'
        initial_amount:
          $ref: '#/components/schemas/InitialAmount'
        initiator_document_number:
          type: string
          description: Payment initiator document number when `type` is `PAYMENT_INITIATOR`
          maxLength: 14
        payment_type:
          $ref: '#/components/schemas/PaymentType'
        psp_account:
          $ref: '#/components/schemas/pix.OutTransferPSPaccount'
        transaction_identification:
          type: string
          description: >
            This field is returned from the [Validate QR Code](ref:post-qr-code)
            endpoint.


            Transaction ID is **REQUIRED** when `initiation_type` is
            `DYNAMIC_QRCODE` and should not be passed otherwise. If a
            `STATIC_QRCODE` contains the transaction identification, it is also
            **REQUIRED** to pass it. 
          example: 1a84daf6-a61b-4aba-919a-a9943db7102e
        metadata:
          $ref: '#/components/schemas/Metadata'
        qr_code_additional_information:
          $ref: '#/components/schemas/QRcodeAddInfo'
        remittance_information:
          maxLength: 140
          type: string
          description: |
            Text payer wents to send to payment receiver.
        urgency:
          $ref: '#/components/schemas/Urgency'
        withdrawal:
          type: object
          description: >
            Withdrawal details. For QR codes and `CHANGE` and `WITHDRAWAL`
            transactions. For example:  a customer goes to a shop, makes a
            purchase, and also asks for cash back.
          properties:
            agent_modality:
              $ref: '#/components/schemas/AgentModality'
            amount:
              description: >-
                Amount's value made available. **REQUIRED** for `CHANGE` or
                `WITHDRAWAL`, cannot be sent for `TRANSFER`.
              example: 101.11
              type: number
            ispb_service_provider:
              type: string
              description: >
                <a
                href="https://bank.codes/numero-do-banco/bank/banco-central-do-brasil/"
                target="_blank">ISPB (Identifcador do Sistema de Pagamento
                Brazil)</a> - the Brazilian Payment System ID, an 8-digit value.
                **REQUIRED** for `CHANGE` or `WITHDRAWAL`.


                This is the ISPB for the withdrawal service provider.
              maxLength: 8
              example: '60701190'
            purchase_amount:
              description: Purchase amount. **REQUIRED** for `CHANGE`.
              example: 101.11
              type: number
        psp_transaction_created_at:
          $ref: '#/components/schemas/PSPtransactionCreatedAt'
        signature_info:
          $ref: '#/components/schemas/pix.SignatureInfo'
        processing_code:
          type: string
          description: >
            A 6-digit code that identifies the financial processing category for
            the transaction, used for accounting and routing purposes.


            This field is optional. When not provided, Pismo automatically
            assigns a value managed internally. If you provide a custom value,
            you take full responsibility for its correctness — an invalid or
            mismatched code may cause processing or reconciliation failures.
          maxLength: 6
          example: '123456'
    pix.OutTransferPSPaccount:
      type: object
      description: >-
        Payment service provider (PSP) information. This object is **REQUIRED**
        if `transaction_type` = `REFUND`.
      required:
        - account_number
        - account_type
        - bank
        - branch
        - document_number
        - name
      properties:
        account_number:
          $ref: '#/components/schemas/AccountNumber'
        account_type:
          $ref: '#/components/schemas/AccountType'
        bank:
          $ref: '#/components/schemas/Bank'
        branch:
          $ref: '#/components/schemas/Branch'
        document_number:
          $ref: '#/components/schemas/DocumentNumber'
        name:
          $ref: '#/components/schemas/Name'
        taxes:
          $ref: '#/components/schemas/Taxes'
    pix.OutTransferResponse:
      type: object
      required:
        - amount
        - end_to_end_id
        - payee_name
        - payer_name
        - status
      properties:
        amount:
          $ref: '#/components/schemas/Amount'
        end_to_end_id:
          $ref: '#/components/schemas/EndToEndID'
        payee_name:
          $ref: '#/components/schemas/Name'
        payer_name:
          $ref: '#/components/schemas/Name'
        status:
          $ref: '#/components/schemas/TransactionStatus'
    pix.Period:
      type: object
      description: Date and times when access is available
      properties:
        maximum_value_available:
          type: string
          description: Maximum value available
          example: '10000'
        weekly_day:
          type: string
          description: Weekly day
          example: Monday
        working_period:
          $ref: '#/components/schemas/pix.WorkingPeriod'
    pix.PixOutScheduleResponse:
      type: object
      required:
        - amount
        - payee
        - payer
        - schedule_creation_datetime
        - schedule_datetime
        - schedule_id
        - status
      properties:
        amount:
          $ref: '#/components/schemas/Amount'
        end_to_end_id:
          $ref: '#/components/schemas/EndToEndID'
        payee:
          type: object
          description: Payee's information.
          properties:
            document_number:
              $ref: '#/components/schemas/DocumentNumber'
            ispb:
              $ref: '#/components/schemas/ISPB'
            name:
              $ref: '#/components/schemas/Name'
        payer:
          type: object
          description: Payer's information.
          properties:
            document_number:
              $ref: '#/components/schemas/DocumentNumber'
            ispb:
              $ref: '#/components/schemas/ISPB'
            name:
              $ref: '#/components/schemas/Name'
        schedule_creation_datetime:
          $ref: '#/components/schemas/ScheduleCreationDatetime'
        schedule_datetime:
          $ref: '#/components/schemas/ScheduleDatetime'
        schedule_id:
          $ref: '#/components/schemas/SchedulerID'
        status:
          $ref: '#/components/schemas/TransferStatus'
    pix.PixOutScheduleRequest:
      required:
        - account_id
        - amount
        - bank_account
        - initiation_type
        - ispb
        - payee
        - schedule_datetime
      type: object
      properties:
        account_id:
          $ref: '#/components/schemas/AccountID'
        amount:
          $ref: '#/components/schemas/Amount'
        bank_account:
          $ref: '#/components/schemas/pix.ScheduleBankAccount'
        initiation_type:
          $ref: '#/components/schemas/ScheduleInitiationType'
        ispb:
          $ref: '#/components/schemas/ISPB'
        payee:
          $ref: '#/components/schemas/pix.SchedulePayee'
        schedule_datetime:
          $ref: '#/components/schemas/ScheduleDatetime'
        remittance_information:
          $ref: '#/components/schemas/RemittanceInformation'
        end_to_end_id:
          $ref: '#/components/schemas/EndToEndID'
    pix.PointAttendance:
      type: object
      properties:
        address:
          type: string
          description: Agent address
          example: Av Augusta, apt 1024
        city:
          type: string
          description: Agent city
          example: Sao Paulo
        latitude:
          type: string
          description: Latitude
          example: 23.5558 S
        longitude:
          type: string
          description: Longitude
          example: 46.6396 W
        name:
          type: string
          description: Name
          example: Jenny Side
        neighborhood:
          type: string
          description: Neighborhood
          example: Bela Vista
        observations:
          type: string
          description: Observations. Any additional text or information.
        period:
          type: array
          items:
            $ref: '#/components/schemas/pix.Period'
        postal_code:
          type: string
          description: Postal code
          example: 01000-000
        products:
          $ref: '#/components/schemas/pix.Products'
        state:
          type: string
          description: State
          example: Rio de Janiero
    pix.Products:
      type: object
      description: Available agent services
      properties:
        pix_change:
          type: boolean
          description: Is Pix change transaction available flag
        pix_withdrawal:
          type: boolean
          description: Is Pix withdrawal transaction available flag
    pix.QRCodeRequest:
      required:
        - emv
      type: object
      properties:
        emv:
          type: string
          description: >
            BR Code in the Europay, Mastercard, Visa (EMV) format. 


            A BR Code is the standard for QR Codes in Brazil and is commonly
            known as *Boleto Bancario*. A vendor or service provider generates a
            Boleto Bancario with the necessary information such as the payer's
            name, the payment amount, the due date, and the vendor's
            information.
          example: >-
            00020126410014BR.GOV.BCB.PIX0119102785947@email.com5204000053039865802BR5915Teste
            do Fulano6009SAO PAULO61080550900062070503***630495F6
        desired_payment_date:
          type: string
          description: >
            Desired payment date, format = yyyy-mm-dd


            This field is used to calculate fees and discounts on QR codes with
            the due date.
          example: '2021-11-18'
    pix.QRCodeResponse:
      type: object
      required:
        - amount
        - end_to_end_id
        - payee
        - qrcode_type
        - transaction_type
      properties:
        additional_information:
          type: array
          description: >
            QR Code additional information. Information generated by payee's
            participant when transaction is a dynamic QR Code process.
          items:
            $ref: '#/components/schemas/commons.AdditionalInfo'
        allows_value_change:
          $ref: '#/components/schemas/AllowsValueChange'
        amount:
          $ref: '#/components/schemas/Amount'
        charges:
          $ref: '#/components/schemas/commons.Charges'
        dates:
          $ref: '#/components/schemas/commons.Dates'
        debtor:
          $ref: '#/components/schemas/commons.Debtor'
        end_to_end_id:
          $ref: '#/components/schemas/EndToEndID'
        initial_amount:
          $ref: '#/components/schemas/InitialAmount'
        payee:
          $ref: '#/components/schemas/commons.Payee'
        payer_solicitation:
          type: string
          description: >-
            Payer solicitation. The message from the receiver to the payer when
            using a QR Code.
          example: Enter fidelity card
        qrcode_type:
          $ref: '#/components/schemas/QRcodeType'
        remittance_information:
          $ref: '#/components/schemas/RemittanceInformation'
        revision:
          type: integer
          description: QR Code revision. If the QR Code is changed, the revision changes.
          example: 1
        status:
          $ref: '#/components/schemas/QRstatus'
        transaction_identification:
          type: string
          description: Creditor party generated transaction ID.
          example: 55d3992a-2f89-46fb-820a-958693bde84d
        transaction_type:
          $ref: '#/components/schemas/TransactionType'
        withdrawal:
          $ref: '#/components/schemas/pix.QRCodeWithdrawResponse'
        recurrence:
          $ref: '#/components/schemas/pix.recurrence'
    pix.recurrence:
      type: object
      properties:
        recurrence_id:
          type: string
          description: Recurrence ID
          example: c80118a7-7cef-4378-a3be-e0e28efbf150
        journey_type:
          $ref: '#/components/schemas/JourneyType'
        contract_id:
          $ref: '#/components/schemas/ContractID'
        amount:
          $ref: '#/components/schemas/Amount'
        min_payment_amount:
          $ref: '#/components/schemas/MinPaymentAmount'
        frequency_type:
          $ref: '#/components/schemas/FrequencyType'
        first_payment_date:
          $ref: '#/components/schemas/FirstPaymentDate'
        last_payment_date:
          $ref: '#/components/schemas/LastPaymentDate'
        payee:
          $ref: '#/components/schemas/Payee'
        debtor:
          $ref: '#/components/schemas/Debtor'
        payer:
          $ref: '#/components/schemas/Payer'
        retry:
          $ref: '#/components/schemas/Retry'
        created_at:
          $ref: '#/components/schemas/CreatedAt'
        read_at:
          $ref: '#/components/schemas/ReadAt'
        history:
          type: array
          description: QRcode history
          items:
            $ref: '#/components/schemas/HistoryItem'
    pix.QRCodeWithdrawResponse:
      type: object
      properties:
        agent_modality:
          $ref: '#/components/schemas/AgentModality'
        allows_value_change:
          $ref: '#/components/schemas/AllowsValueChange'
        amount:
          $ref: '#/components/schemas/Amount'
        ispb_service_provider:
          $ref: '#/components/schemas/ISPB'
        purchase_amount:
          $ref: '#/components/schemas/PurchaseAmount'
    pix.ReceiptAmountCharges:
      type: object
      properties:
        discount_value:
          $ref: '#/components/schemas/DiscountValue'
        interest_value:
          $ref: '#/components/schemas/InterestValue'
        fine_value:
          $ref: '#/components/schemas/FineValue'
        settled_value:
          $ref: '#/components/schemas/SettledValue'
    pix.ReceiptDebtor:
      type: object
      properties:
        document_number:
          $ref: '#/components/schemas/DocumentNumber'
        name:
          $ref: '#/components/schemas/Name'
    pix.ReceiptFindResponse:
      type: object
      properties:
        items:
          type: array
          description: Receipt list
          items:
            $ref: '#/components/schemas/pix.ReceiptFindResponseItem'
        last_id:
          $ref: '#/components/schemas/LastID'
        next_page:
          $ref: '#/components/schemas/NextPage'
        per_page:
          $ref: '#/components/schemas/PerPage'
    pix.ReceiptFindResponseItem:
      type: object
      properties:
        amount:
          $ref: '#/components/schemas/Amount'
        purchase_amount:
          $ref: '#/components/schemas/PurchaseAmount'
        withdraw_amount:
          type: number
          description: Amount to be withdrawn.
          example: 100
        transaction_identification:
          type: string
          description: Creditor generated transaction ID.
          example: d4c38871c8354f0fbe91b3c9c7d5b929
        type:
          $ref: '#/components/schemas/PixOutType'
        authorization_datetime:
          type: string
          description: >-
            Authorization datetime. RFC 3339 value, i.e.,
            "2023-04-12T23:20:50.52Z".
          example: '2021-10-11T19:01:43.392Z'
        charges:
          $ref: '#/components/schemas/pix.ReceiptAmountCharges'
        debtor:
          $ref: '#/components/schemas/pix.ReceiptDebtor'
        description:
          type: string
          description: Description.
          example: Transaction Pix
        end_to_end_id:
          $ref: '#/components/schemas/EndToEndID'
        initial_amount:
          $ref: '#/components/schemas/InitialAmount'
        initiation_type:
          $ref: '#/components/schemas/InitiationType'
        transaction_type:
          $ref: '#/components/schemas/TransactionType'
        initiator_document_number:
          $ref: '#/components/schemas/DocumentNumber'
        internal:
          $ref: '#/components/schemas/Internal'
        payee:
          $ref: '#/components/schemas/pix.ReceiptPayee'
        payer:
          $ref: '#/components/schemas/pix.ReceiptPayer'
        qrcode_additional_information:
          type: array
          description: >-
            QRCode Additional information. Information generated by payee's
            participant when transaction is a Dynamic QR Code process.
          items:
            $ref: '#/components/schemas/pix.ReceiptQRCodeAdditionalInformation'
        reversal:
          $ref: '#/components/schemas/pix.ReceiptReversal'
        tx_id:
          type: string
          description: >-
            Tax ID generated in the static QR Code, intended for the final
            user's financial reconciliation.
          example: d4c38871c8354f0fbe91b3c9c7d5b929
        taxes:
          $ref: '#/components/schemas/Taxes'
    pix.ReceiptPayee:
      type: object
      properties:
        account_number:
          $ref: '#/components/schemas/AccountNumber'
        branch:
          $ref: '#/components/schemas/Branch'
        document_number:
          $ref: '#/components/schemas/DocumentNumber'
        ispb:
          $ref: '#/components/schemas/ISPB'
        name:
          $ref: '#/components/schemas/Name'
    pix.ReceiptPayer:
      type: object
      properties:
        account_number:
          $ref: '#/components/schemas/AccountNumber'
        branch:
          $ref: '#/components/schemas/Branch'
        document_number:
          $ref: '#/components/schemas/DocumentNumber'
        ispb:
          $ref: '#/components/schemas/ISPB'
        name:
          $ref: '#/components/schemas/Name'
    pix.ReceiptQRCodeAdditionalInformation:
      type: object
      properties:
        name:
          type: string
          description: QR Code name information.
          example: Discount
        value:
          type: string
          description: QR Code value information.
          example: Paying until the 5th, there will be a 10% discount
    pix.ReceiptReversal:
      type: object
      properties:
        amount:
          $ref: '#/components/schemas/Amount'
        authorization_datetime:
          type: string
          description: >-
            Reversal authorization datetime. RFC 3339 value, i.e.,
            "2023-04-12T23:20:50.52Z".
          example: '2021-10-11T19:01:43.392Z'
        end_to_end_id:
          $ref: '#/components/schemas/EndToEndID'
        reason:
          $ref: '#/components/schemas/ReversalReason'
    pix.Records:
      type: object
      description: Agent information
      properties:
        name_agent_withdrawal:
          type: string
          description: Withdrawal agent name
          example: Ruth Les
        point_attendance:
          type: array
          description: >-
            Where WSA service point is available - bank branches, ATMs, stores,
            and so on.
          items:
            $ref: '#/components/schemas/pix.PointAttendance'
        tax_id_agent_withdrawal:
          type: string
          description: Withdrawal agent tax ID
          example: '123412341234'
    pix.ScheduleBankAccount:
      type: object
      required:
        - account_number
        - account_type
        - branch
      properties:
        account_number:
          $ref: '#/components/schemas/AccountNumber'
        account_type:
          $ref: '#/components/schemas/AccountType'
        branch:
          $ref: '#/components/schemas/Branch'
    pix.SchedulePayee:
      type: object
      required:
        - account_number
        - account_type
        - bank
        - branch
        - document_number
        - name
      properties:
        account_number:
          $ref: '#/components/schemas/AccountNumber'
        account_type:
          $ref: '#/components/schemas/AccountType'
        bank:
          $ref: '#/components/schemas/Bank'
        branch:
          $ref: '#/components/schemas/Branch'
        document_number:
          $ref: '#/components/schemas/DocumentNumber'
        name:
          $ref: '#/components/schemas/Name'
        key:
          type: string
          description: Key value. Cannot be passed when `initiation_type` is `MANUAL`
          example: 45fe6866-21a5-4f6d-84c2-950a2a1df8e7
        beneficiary_id:
          $ref: '#/components/schemas/BeneficiaryID'
    pix.SchedulesFindResponse:
      required:
        - per_page
        - total_items
      type: object
      properties:
        items:
          description: Page list of items.
          type: array
          items:
            $ref: '#/components/schemas/pix.SimpleScheduledTransfer'
        last_evaluated_creation_datetime:
          type: string
          description: >
            Last evaluated creation date time. Used to fetch the next page of
            results.


            RFC 3339 value, i.e., "2023-04-12T23:20:50.52Z". 
        per_page:
          $ref: '#/components/schemas/PerPage'
        total_items:
          $ref: '#/components/schemas/TotalItems'
    pix.SignatureInfo:
      type: object
      description: >
        Signature information, including the digital signature over the request
        data that <a
        href="https://www.legislation.gov.uk/ukpga/2022/46/contents" that a PSTI
        validates. A Prestadores de Serviços de Tecnologia da Informação (PSTI)
        - Information and Technology Service Provider in English — is a
        technology service provider that connects to the banking/financial
        infrastructure (specifically the Rede do Sistema Financeiro Nacional –
        RSFN) on participants' behalf.


        The signature here is the digital signature standard using a private
        asymmetric key (<a
        href="https://www.geeksforgeeks.org/computer-networks/rsa-algorithm-cryptography/"
        target="_blank">RSA</a> or <a
        href="https://en.wikipedia.org/wiki/Elliptic-curve_cryptography"
        target="_blank">Elliptic Curve</a>) over an input buffer extracted from
        the request and provider-defined. 


        This field guarantees the request's integrity and authenticity and the
        transaction messaging non-repudiation (assurance that a party involved
        in a transaction cannot later deny their involvement) from customer to
        PSTI.


        For more information, refer to the  [Scheduled Pix-out transfer
        encryption](doc:pix-out-transfer-encryption) guide.


        **Note:** Customers should contact the PSTI to get more information
        about the encryption process and how to store the certificate related to
        the certificate thumbprint. Pismo acts as a pass-through entity in this
        process.
      required:
        - algorithm
        - certificate_thumbprint
        - signature
      properties:
        algorithm:
          type: string
          description: >
            The <a
            href="https://en.wikipedia.org/wiki/Digital_Signature_Algorithm"
            target="_blank">digital signature algorithm</a> (DSA). 
          enum:
            - ECDSA_P256_SHA256
            - ECDSA_P384_SHA384
            - Ed25519
            - RSA_PKCS1_SHA256
            - RSA_PSS_SHA256
          example: RSA_PKCS1_SHA256
        certificate_thumbprint:
          type: string
          description: >-
            The certificate thumbprint calculated using a <a
            href="https://en.wikipedia.org/wiki/SHA-2" target="_blank">SHA-256
            algorithm</a> and encoded in hexadecimal.
          maxLength: 512
          example: 3a2b4c1b...
        signature:
          type: string
          description: >
            The digital signature calculated over the payload (see the input
            buffer data and provider-defined format) according to the DSA
            standard and Base64 encoded.
          maxLength: 1024
          example: MIIb2132b==
    pix.SimpleScheduledTransfer:
      required:
        - amount
        - payee
        - schedule_creation_datetime
        - schedule_datetime
        - schedule_id
        - status
      type: object
      properties:
        amount:
          $ref: '#/components/schemas/Amount'
        cancel_datetime:
          description: >-
            Scheduled transfer cancellation datetime. RFC 3339 value, i.e.,
            "2023-04-12T23:20:50.52Z".
          type: string
          example: '2020-08-13T13:49:03.000Z'
        end_to_end_id:
          $ref: '#/components/schemas/EndToEndID'
        internal:
          $ref: '#/components/schemas/Internal'
        payee:
          type: object
          description: Payee's information.
          required:
            - document_number
            - ispb
            - name
          properties:
            document_number:
              $ref: '#/components/schemas/DocumentNumber'
            ispb:
              $ref: '#/components/schemas/ISPB'
            name:
              $ref: '#/components/schemas/Name'
        schedule_creation_datetime:
          $ref: '#/components/schemas/ScheduleCreationDatetime'
        schedule_datetime:
          $ref: '#/components/schemas/ScheduleDatetime'
        schedule_id:
          $ref: '#/components/schemas/SchedulerID'
        status:
          $ref: '#/components/schemas/TransferStatus'
    pix.TransactionCharge:
      type: object
      properties:
        discount_value:
          $ref: '#/components/schemas/DiscountValue'
        fine_value:
          $ref: '#/components/schemas/FineValue'
        interest_value:
          $ref: '#/components/schemas/InterestValue'
        settlement_value:
          type: number
          description: Settlement value
    pix.TransactionDates:
      type: object
      properties:
        created_datetime:
          type: string
          description: Date/time created on Pismo platform. RFC 3339 format.
          example: '2021-10-11T19:01:43.392Z'
        psp_transaction_created_at:
          type: string
          description: >-
            PSP-generated transaction date/time. If not passed, Pismo generates
            this field upon request. RFC 3339 format.
          example: '2021-10-12T19:01:43.392Z'
        received_datetime:
          type: string
          description: Date/time when Pismo received request.
          example: '2021-10-12T19:01:43.392Z'
        updated_datetime:
          type: string
          description: Last update date/time. RFC 3339 format.
          example: '2021-10-12T19:01:43.392Z'
    pix.TransactionExternalAccount:
      type: object
      properties:
        bank_account:
          $ref: '#/components/schemas/pix.TransactionExternalBankAccount'
        document:
          $ref: '#/components/schemas/DocumentNumber'
        name:
          $ref: '#/components/schemas/Name'
    pix.TransactionExternalBankAccount:
      type: object
      description: Bank account details.
      required:
        - account_number
        - account_type
        - branch
      properties:
        account_number:
          $ref: '#/components/schemas/AccountNumber'
        account_type:
          $ref: '#/components/schemas/AccountType'
        branch:
          $ref: '#/components/schemas/Branch'
    pix.TransactionSearchRequest:
      type: object
      required:
        - client_request_id
      properties:
        client_request_id:
          $ref: '#/components/schemas/ClientRequestID'
    pix.TransactionWithDetailResponse:
      type: object
      properties:
        end_to_end_id:
          $ref: '#/components/schemas/EndToEndID'
        org_id:
          type: string
          description: Organization/tenant ID.
          example: TN-1234...
        account_id:
          $ref: '#/components/schemas/AccountID'
        ispb:
          $ref: '#/components/schemas/ISPB'
        type:
          $ref: '#/components/schemas/PixOutType'
        transaction_type:
          $ref: '#/components/schemas/TransactionType'
        initiation_type:
          $ref: '#/components/schemas/InitiationType'
        status:
          $ref: '#/components/schemas/TransactionStatus'
        correlation_id:
          type: string
          description: Correlation tracking ID (CID)
          example: '8675309'
        authorization_id:
          $ref: '#/components/schemas/AuthorizationID'
        client_request_id:
          $ref: '#/components/schemas/ClientRequestID'
        payee:
          $ref: '#/components/schemas/pix.TransactionExternalAccount'
        payer:
          $ref: '#/components/schemas/pix.TransactionExternalAccount'
        initiator_document_number:
          type: string
          description: Payment initiator document number when `type` is `PAYMENT_INITIATOR`
          maxLength: 14
          example: '12345678900'
        initial_amount:
          $ref: '#/components/schemas/InitialAmount'
        amount:
          $ref: '#/components/schemas/Amount'
        internal:
          $ref: '#/components/schemas/Internal'
        additional_description:
          type: string
          description: Additional description
          example: 'Charge #1'
        qr_code_additional_information:
          $ref: '#/components/schemas/QRcodeAddInfo'
        transaction_identification:
          $ref: '#/components/schemas/TransactionID'
        reason:
          $ref: '#/components/schemas/ReversalReason'
        external_id:
          type: string
          description: Provider external ID
          example: '1236723'
        external_movement_id:
          type: string
          description: Provider external movement identification
          example: '12345678'
        migration_id:
          type: string
          description: Transaction migration ID (if migrated).
          example: c80118a7-7cef-4378-a3be-e0e28efbf150
        charges:
          $ref: '#/components/schemas/pix.TransactionCharge'
        dates:
          $ref: '#/components/schemas/pix.TransactionDates'
        taxes:
          $ref: '#/components/schemas/Taxes'
    pix.ValidateTransmitterWithdrawalResponse:
      type: object
      properties:
        is_transmitter_withdrawal:
          type: boolean
          description: Is this a withdrawal servicer flag
    pix.WithdrawalServicePointResponse:
      type: object
      properties:
        meta:
          $ref: '#/components/schemas/pix.Meta'
        records:
          type: array
          items:
            $ref: '#/components/schemas/pix.Records'
    pix.WorkingPeriod:
      type: object
      properties:
        closing_withdrawal:
          type: string
          description: Closing withdrawal time
          example: '18:00'
        opening_withdrawal:
          type: string
          description: Opening withdrawal time
          example: '08:00'
    PixOutType:
      type: string
      description: Pix transaction type.
      enum:
        - PIX-IN
        - PIX-OUT
        - PIX-IN-REVERSAL
        - PIX-OUT-REVERSAL
      example: PIX-IN
    PostalCode:
      type: string
      description: Postal/zip code
      example: '8675309'
    ProcessingType:
      type: string
      description: Processing type
      enum:
        - ONCE
        - RECURRING
      example: RECURRING
    PSPtransactionCreatedAt:
      type: string
      description: >
        Datetime since the transaction was created on client-side.


        Format = RFC 3339 Nano.  For example -
        `2014-03-19T00:08.179780629-04:00`.

         **REQUIRED** when passing `signature_info`
      example: 2014-03-19T00:08.179780629-04:00
    PurchaseAmount:
      type: number
      description: Purchase price before charges, withdrawal changes, or other changes.
      example: 1050.44
    QRcodeAddInfo:
      type: array
      description: Additional QR Code information.
      items:
        type: object
        properties:
          name:
            type: string
            description: QR Code name
            example: Discount
          value:
            type: string
            description: QR Code value
            example: Paying until the 5th, there will be a 10% discount
    QRcodeType:
      description: |
        QR Code type: 
        `STATIC` - Static QR Code
        `STATIC_WITHDRAW` - Static withdrawal
        `DYNAMIC_COB` - Dynamic QR Code with charge 
        `DYNAMIC_COBV` - Dynamic QR Code with charge that has due date
        `DYNAMIC_CHANGE`- Dynamic QR Code change
        `DYNAMIC_WITHDRAW` - Dynamic QR withdrawal
      type: string
      enum:
        - STATIC
        - STATIC_WITHDRAW
        - DYNAMIC_COB
        - DYNAMIC_COBV
        - DYNAMIC_CHANGE
        - DYNAMIC_WITHDRAW
      example: DYNAMIC_COB
    QRstatus:
      type: string
      description: |
        QR Code status: 
        `ATIVA` - Active 
        `CONCLUDIA` - Concluded 
        `REMOVIDA_PELO_USUARIO_RECEBEDOR` - Removed by the receiving user
        `REMOVIDA_PELO_PSP` - Removed by PSP
      enum:
        - ATIVA
        - CONCLUIDA
        - REMOVIDA_PELO_USUARIO_RECEBEDOR
        - REMOVIDA_PELO_PSP
      example: ATIVA
    ReadAt:
      type: string
      description: Datetime QR Code was read, RFC 3339 format
      example: '2024-11-14T17:54:40Z'
    Records:
      type: object
      properties:
        tax_type:
          $ref: '#/components/schemas/TaxType'
        amount:
          $ref: '#/components/schemas/TaxAmount'
    RecurrenceID:
      type: string
      description: BACEN recurrence ID
      example: RN000000012025050500012345678
    RecurrenceStatus:
      type: string
      description: "\nRecurrence status\n\n  * `CREATED`\t  - Recurrence created, waiting on journey acceptance.\n  * `ACCEPTED`  - Payer accepted recurrence, awaiting confirmation from receiver's PSP.\n  * `APPROVED`  - Payer accepted recurrence and receiver's PSP confirmed. \n  * `REJECTED`  - Payer or receiver's PSP rejected recurrence during `ACCEPTED` validation. For example: receiver's account is inactive.\n  * `EXPIRED`   - Recurrence expired after last scheduled payment date.\n  * `CANCELLED` - Payer or reciever cancelled recurrence.\n  * `FINISHED`  - Deprecated\n"
      enum:
        - CREATED
        - ACCEPTED
        - APPROVED
        - REJECTED
        - EXPIRED
        - CANCELLED
        - FINISHED
      example: REJECTED
    RemittanceInformation:
      type: string
      description: >
        Remittance information. Information from payee when transaction is a
        static QR Code.
      maxLength: 140
    Retry:
      type: boolean
      description: Retry transaction on failure?
      example: true
    ReversalReason:
      type: string
      description: Reversal reason.
      enum:
        - USER_REQUESTED
        - FRAUD
        - BANK_ERROR
      example: USER_REQUESTED
    ScheduleCreationDatetime:
      type: string
      description: >-
        Scheduled transfer creation datetime. RFC 3339 value, i.e.,
        "2023-04-12T23:20:50.52Z".
      example: '2020-08-13T13:49:03.000Z'
    ScheduleDatetime:
      type: string
      description: >-
        Date to schedule the process. RFC 3339 value, i.e.,
        "2023-04-12T23:20:50.52Z".
      example: '2020-08-13T13:49:03.000Z'
    scheduler.BankAccount:
      type: object
      required:
        - account_number
        - account_type
        - branch
      properties:
        account_type:
          $ref: '#/components/schemas/AccountType'
        branch:
          $ref: '#/components/schemas/Branch'
        account_number:
          $ref: '#/components/schemas/AccountNumber'
    scheduler.CreatedAt:
      type: string
      description: Scheduled item creation time, RFC 3339 format
      example: '2024-11-14T17:54:40Z'
    scheduler.ExecutedAt:
      type: string
      description: Scheduled item execution time, RFC 3339 format
      example: '2024-11-14T17:54:40Z'
    scheduler.Execution:
      type: object
      properties:
        check_date_time:
          type: string
          description: Datetime check_interval processed. RFC 3339 format.
          example: '2021-10-11T00:00:00.000Z'
        end_to_end_id:
          $ref: '#/components/schemas/EndToEndID'
        executed_at:
          $ref: '#/components/schemas/scheduler.ExecutedAt'
        execution_date_time:
          type: string
          description: When the scheduler task was scheduled to run. RFC 3339 format.
          example: '2021-10-11T00:00:00.000Z'
        execution_id:
          $ref: '#/components/schemas/ExecutionID'
        retry_date_time:
          type: string
          description: Datetime to retry transfer after error. RFC 3339 format.
          example: '2021-10-11T00:00:00.000Z'
        status:
          $ref: '#/components/schemas/SchedulerStatus'
    scheduler.Executiona:
      type: object
      required:
        - execution_datetime
        - execution_id
        - status
      properties:
        execution_id:
          $ref: '#/components/schemas/ExecutionID'
        execution_datetime:
          type: string
          description: When the scheduler task was scheduled to run. RFC 3339 format.
          example: '2021-10-11T00:00:00.000Z'
        status:
          $ref: '#/components/schemas/ExecutionStatus'
        check_datetime:
          type: string
          description: Datetime check_interval processed. RFC 3339 format.
          example: '2021-10-11T00:00:00.000Z'
        created_at:
          $ref: '#/components/schemas/scheduler.CreatedAt'
        executed_at:
          $ref: '#/components/schemas/scheduler.ExecutedAt'
        retry_datetime:
          type: string
          description: Retry datetime, RFC3339 format
          example: '2021-10-11T00:00:00.000Z'
        updated_at:
          $ref: '#/components/schemas/UpdatedAt'
        error:
          $ref: '#/components/schemas/scheduler.ExecutionError'
    scheduler.ExecutionError:
      type: object
      properties:
        code:
          type: string
          description: Error code
        message:
          type: string
          description: Error message
    scheduler.ExecutionsListResponse:
      type: object
      properties:
        last_id:
          $ref: '#/components/schemas/LastID'
        next_page:
          $ref: '#/components/schemas/NextPage'
        number_elements:
          $ref: '#/components/schemas/NumberElements'
        page_size:
          $ref: '#/components/schemas/PageSize'
        items:
          type: array
          items:
            $ref: '#/components/schemas/scheduler.Executiona'
    scheduler.ItemQueryResponse:
      type: object
      required:
        - day_type
        - end_date
        - interval
        - scheduler_id
        - start_date
        - status
        - type
      properties:
        day_type:
          type: string
          description: Day type
          enum:
            - BUSINESS
            - CALENDAR
          example: CALENDAR
        cycle:
          $ref: '#/components/schemas/SchedulerCycle'
        is_device_registered:
          $ref: '#/components/schemas/IsDeviceRegistered'
        end_date:
          type: string
          description: End date, format = yyyy-mm-dd
          example: '2025-11-14'
        start_date:
          type: string
          description: Start date, format = yyyy-mm-dd
          example: '2024-11-14'
        interval:
          $ref: '#/components/schemas/Interval'
        scheduler_id:
          $ref: '#/components/schemas/SchedulerID'
        status:
          $ref: '#/components/schemas/SchedulerStatus2'
        type:
          $ref: '#/components/schemas/ProcessingType'
        amount:
          $ref: '#/components/schemas/Amount'
        description:
          type: string
          description: Item description
          example: Weekly payment
        continue_even_failed:
          type: boolean
          description: Continue if failed flag
          example: true
        execution_hour:
          type: string
          description: Scheduled execution hour, format = HH:MM:SS
          example: '17:56:00'
        retry_hour:
          type: string
          description: Scheduled retry hour, format = HH:MM:SS
          example: '20:00:00'
        check_interval:
          type: string
          description: Check scheduler interval in minutes.
          example: '10'
        created_at:
          $ref: '#/components/schemas/scheduler.CreatedAt'
        updated_at:
          $ref: '#/components/schemas/UpdatedAt'
    scheduler.Payee:
      type: object
      required:
        - account_number
        - account_type
        - bank
        - branch
        - name
        - document_number
      properties:
        account_number:
          $ref: '#/components/schemas/AccountNumber'
        account_type:
          $ref: '#/components/schemas/AccountType'
        bank:
          $ref: '#/components/schemas/Bank'
        branch:
          $ref: '#/components/schemas/Branch'
        beneficiary_id:
          $ref: '#/components/schemas/BeneficiaryID'
        document_number:
          $ref: '#/components/schemas/DocumentNumber'
        name:
          $ref: '#/components/schemas/Name'
        key:
          $ref: '#/components/schemas/Key'
        type:
          $ref: '#/components/schemas/PersonType'
    scheduler.PayeeResponse:
      type: object
      properties:
        name:
          $ref: '#/components/schemas/Name'
        document_number:
          $ref: '#/components/schemas/DocumentNumber'
        ispb:
          $ref: '#/components/schemas/ISPB'
    scheduler.PayerResponse:
      type: object
      properties:
        name:
          $ref: '#/components/schemas/Name'
        document_number:
          $ref: '#/components/schemas/DocumentNumber'
        ispb:
          $ref: '#/components/schemas/ISPB'
    scheduler.SchedulerDetails:
      type: object
      required:
        - end_scheduler_date
        - start_scheduler_date
        - type
      properties:
        type:
          $ref: '#/components/schemas/ProcessingType'
        description:
          type: string
          description: Schedule description
          maxLength: 200
          example: Pay at end of month
        start_scheduler_date:
          type: string
          description: >-
            Datetime to start the scheduled payment. Format = yyyy-mm-dd.
            Transfers are made at midnight on this date, and if recurring, every
            month until `end_scheduler_date`.
          example: '2024-06-10'
        end_scheduler_date:
          type: string
          description: >-
            Datetime to stop the scheduled payment (maximum = 1 year). Format =
            yyyy-mm-dd
          example: '2024-06-10'
        cycle:
          $ref: '#/components/schemas/SchedulerCycle'
    scheduler.SchedulerRegisterRequest:
      type: object
      required:
        - amount
        - bank_account
        - initiation_type
        - ispb
        - payee
        - scheduler_details
      properties:
        amount:
          $ref: '#/components/schemas/Amount'
        bank_account:
          $ref: '#/components/schemas/scheduler.BankAccount'
        initiation_type:
          $ref: '#/components/schemas/InitiationType2'
        ispb:
          $ref: '#/components/schemas/ISPB'
        is_device_registered:
          $ref: '#/components/schemas/IsDeviceRegistered'
        payee:
          $ref: '#/components/schemas/scheduler.Payee'
        qr_code_additional_information:
          $ref: '#/components/schemas/QRcodeAddInfo'
        scheduler_details:
          $ref: '#/components/schemas/scheduler.SchedulerDetails'
        end_to_end_id:
          $ref: '#/components/schemas/EndToEndID'
        remittance_information:
          $ref: '#/components/schemas/RemittanceInformation'
        transaction_identification:
          type: string
          description: >
            Transaction ID. **REQUIRED** if `initiation_type` =
            `DYNAMIC_QR_CODE`. Cannot be passed if  `MANUAL` or `DICT`. If
            `STATIC_QR_CODE` contains `transaction_identification`, it is
            **REQUIRED** to pass it.
          example: 1a84daf6-a61b-4aba-919a-a9943db7102e
        flex_controls:
          $ref: '#/components/schemas/FlexControls'
        taxes:
          $ref: '#/components/schemas/Taxes'
    scheduler.SchedulerRegisterResponse:
      type: object
      properties:
        scheduler_id:
          $ref: '#/components/schemas/SchedulerID'
    scheduler.SchedulerSearchQueryResponse2:
      type: object
      properties:
        next_page:
          $ref: '#/components/schemas/NextPage'
        last_id:
          $ref: '#/components/schemas/LastID'
        page_size:
          $ref: '#/components/schemas/PageSize'
        number_elements:
          $ref: '#/components/schemas/NumberElements'
        filters:
          type: array
          items:
            $ref: '#/components/schemas/scheduler.ItemQueryFilterResponse'
        sorts:
          type: array
          items:
            $ref: '#/components/schemas/scheduler.ItemQuerySortResponse'
        items:
          type: array
          items:
            $ref: '#/components/schemas/scheduler.ItemQueryResponse'
    scheduler.ItemQueryFilterResponse:
      type: object
      properties:
        label:
          type: string
          description: Field name
          example: status
        value:
          type: string
          description: Field's value
          example: ACTIVE
        operator:
          type: string
          description: Operator type
          example: EQUAL
    scheduler.ItemQuerySortResponse:
      type: object
      properties:
        field:
          type: string
          description: Field name
          example: status
        order:
          type: string
          description: Order
          enum:
            - ASC
            - DESC
          example: ASC
    scheduler.SchedulerSearchResponse:
      required:
        - scheduler_id
      type: object
      properties:
        scheduler_id:
          $ref: '#/components/schemas/SchedulerID'
        scheduler_creation_datetime:
          type: string
          description: Datetime when scheduled transfer created. RFC 3339 format.
          example: '2021-10-11T00:00:00.000Z'
        scheduler_lastupdate_datetime:
          type: string
          description: Datetime when scheduled transfer last updated. RFC 3339 format.
          example: '2021-10-11T00:00:00.000Z'
        scheduler_details:
          $ref: '#/components/schemas/scheduler.SchedulerDetails'
        amount:
          $ref: '#/components/schemas/Amount'
        status:
          $ref: '#/components/schemas/SchedulerStatus'
        is_device_registered:
          $ref: '#/components/schemas/IsDeviceRegistered'
        payee:
          $ref: '#/components/schemas/scheduler.PayeeResponse'
        payer:
          $ref: '#/components/schemas/scheduler.PayerResponse'
        next_execution:
          $ref: '#/components/schemas/scheduler.Execution'
        executions_preview:
          $ref: '#/components/schemas/scheduler.ExecutionsPreview'
    scheduler.ExecutionsPreview:
      type: object
      description: >-
        Preview of upcoming executions that will be created in the future.
        Present only when `showPreview=true`.
      properties:
        has_more:
          type: boolean
          description: Are there more than 50 upcoming executions?
          example: false
        items:
          type: array
          maxItems: 50
          description: List of upcoming executions preview, limited to 50 items.
          items:
            $ref: '#/components/schemas/scheduler.PreviewExecution'
    scheduler.PreviewExecution:
      type: object
      description: A single upcoming execution in the preview.
      properties:
        execution_id:
          $ref: '#/components/schemas/ExecutionID'
        date:
          type: string
          description: Scheduled date for the upcoming execution. RFC 3339 format.
          example: '2025-01-15T00:00:00.000Z'
    SchedulerCycle:
      type: object
      description: >
        Cycle property present for scheduler `type` = `RECURRING`. Defaults to
        `MONTHLY`.
      properties:
        type:
          type: string
          description: Recurring cycle type
          enum:
            - DAILY
            - WEEKLY
            - MONTHLY
            - YEARLY
          example: MONTHLY
        count:
          type: integer
          description: Cycle count for generating new transactions
          example: 1
    SchedulerID:
      type: string
      description: Scheduled PIX transaction ID
      example: 7b29a055-00e1-41e5-b880-aebe517b3bf6
    SchedulerStatus:
      type: string
      description: Scheduler status
      enum:
        - DELIVERED
        - PROCESSING
        - CANCELLED
        - SUCCESS
        - FAIL
        - PENDING
      example: DELIVERED
    SchedulerStatus2:
      type: string
      description: Scheduler status
      enum:
        - ACTIVE
        - INACTIVE
        - CANCELLED
        - COMPLETED
      example: COMPLETED
    SettledValue:
      type: number
      description: >-
        Settlement value. Amount to transfer after all charges have been
        applied.
      example: 10.15
    TaxAmount:
      type: number
      description: Tax amount. Must be greater than zero.
      example: 1050.44
    Taxes:
      type: object
      description: >
        Only available in the EXT environment for direct participants. Tax
        information related to the transaction.
      properties:
        tax_identifier:
          $ref: '#/components/schemas/TaxIdentifier'
        records:
          type: array
          description: >
            Tax record types information. You must pass, at a minimum, 2 objects
            - at least one with `CBS` tax type and at least one with `IBS` tax
            type.
          items:
            $ref: '#/components/schemas/Records'
    TaxIdentifier:
      type: string
      description: >
        Tax identifier, referring to the NFe, NFSe or aggregating tax document. 


        In Brazil, NF-e and NFS-e are the two primary types of mandatory
        electronic invoices (Notas Fiscais) used for tax compliance.

         * NF-e (Nota Fiscal Eletrônica): Used for the sale and movement of physical goods or products. It is regulated at the state level by SEFAZ (SEFAZ (Secretaria da Fazenda) is the State Department of Finance in Brazil.) and travels with the goods.

         * NFS-e (Nota Fiscal de Serviços Eletrônica): Used for the provision of services (e.g., consulting, repairs, digital subscriptions). It is regulated by individual city governments.

        The value entered must correspond to the tax document used for
        calculating the IBS and CBS taxes associated with the transaction.
      example: IS0435879820240605bcdb1fhn43a
      maxLength: 50
    TaxType:
      type: string
      description: >
        Tax type:

         `IBS` = Goods and services tax. A state and municipal tax that unifies and replaces the main subnational consumption taxes (state ICMS and municipal ISS).

        `CBS` = Contribution on goods and services. A federal tax that
        consolidates and replaces previous federal social contributions and
        taxes (PIS, Cofins, and IPI).
      example: IBS
      enum:
        - CBS
        - IBS
    TotalItems:
      type: integer
      description: Total number of items
      example: 1000
    TransactionID:
      type: string
      description: >
        This field is returned from the [Validate QR Code](ref:post-qr-code)
        endpoint.


        Transaction ID is **REQUIRED** when `initiation_type` is
        `DYNAMIC_QRCODE` and should not be passed otherwise. If a
        `STATIC_QRCODE` contains the transaction identification, it is also
        **REQUIRED** to pass it. 
      example: 1a84daf6-a61b-4aba-919a-a9943db7102e
    TransactionStatus:
      type: string
      description: |
        Current transaction status:

          * `VALIDATED` - Payload was validated and sent to provider
            
          * `PROCESSING` - Waiting response from the provider
            
          * `SETTLED` - Transaction confirmed
            
          * `CANCELLED` - Transaction cancelled
            
          * `ERROR` - Transaction error during processing
      enum:
        - PROCESSING
        - VALIDATED
        - SETTLED
        - CANCELLED
        - ERROR
      example: PROCESSING
    TransactionType:
      type: string
      description: >
        Type of transaction the QR Code validation generates.

        * `TRANSFER` - Standard immediate transfer payment. 

        * `CHANGE` - Occurs when the QR Code validation returns `CHANGE`. For
        example, the customer buys something and pays more than the product
        value to receive the difference in cash. 

        * `WITHDRAWAL` - Occurs when the QR Code validation returns `WITHDRAWAL`
        with `withdrawal` object values.

        * `REFUND_AUTOMATIC_PIX` - Total or partial to the PSP payer user within
        the Automatic Pix MED (Pix return mechanism) scope (only when initiated
        by a `MANUAL`).

        * `REFUND` = Total or partial refund to the paying user within the scope
        of MED 2.0, when this refund occurs from the PI Account of the user's
        PSP who participated in the distribution chain of the resources of the
        original transaction (only when initiated by a `MANUAL`)
      enum:
        - TRANSFER
        - CHANGE
        - WITHDRAWAL
        - REFUND_AUTOMATIC_PIX
        - REFUND
      example: TRANSFER
    TransferStatus:
      type: string
      description: Scheduled transfer status.
      example: SCHEDULED
      enum:
        - SCHEDULED
        - CANCELLED
        - SETTLED
        - FAILED
    UpdatedAt:
      type: string
      description: Scheduled item last updated time, RFC 3339 format
      example: '2024-11-14T17:54:40Z'
    Urgency:
      type: string
      description: ' **REQUIRED** when `initiation_type` = `PAYMENT_INITIATOR`. Must be `NORMAL` in this case. Default is `HIGH`.'
      enum:
        - NORMAL
        - HIGH
      example: HIGH
  responses:
    400BadRequest:
      description: Bad request
      headers:
        x-cid:
          description: Request tracking ID.
          schema:
            type: string
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/customer.Error'
    400BadRequest2:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/customer.Error'
    403Forbidden:
      description: Forbidden
      headers:
        x-cid:
          description: Request tracking ID.
          schema:
            type: string
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/customer.Error'
    403Forbidden2:
      description: Forbidden
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/customer.Error'
    404NotFound:
      description: Not found
      headers:
        x-cid:
          description: Request tracking ID.
          schema:
            type: string
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/customer.Error'
    404NotFound2:
      description: Not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/customer.Error'
    409Conflict:
      description: Conflict
      headers:
        x-cid:
          description: Request tracking ID.
          schema:
            type: string
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/customer.Error'
    429TooManyRequests:
      description: Too many requests
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/customer.Error'
    429TooManyRequests2:
      description: Too many requests
      headers:
        x-cid:
          description: Request tracking ID.
          schema:
            type: string
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/customer.Error'
    500InternalServer:
      description: Internal Server Error
      headers:
        x-cid:
          description: Request tracking ID.
          schema:
            type: string
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/customer.Error'
    500InternalServer2:
      description: Internal Server Error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/customer.Error'
    502BadGateway:
      description: Bad gateway
      headers:
        x-cid:
          description: Request tracking ID.
          schema:
            type: string
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/customer.Error'
    502BadGateway2:
      description: Bad gateway
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/customer.Error'
    504GatewayTimeout:
      description: Gateway timeout
      headers:
        x-cid:
          description: Request tracking ID.
          schema:
            type: string
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/customer.Error'
    504GatewayTimeout2:
      description: Gateway timeout
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/customer.Error'
paths:
  /pix/v1/pix/out/transfer:
    post:
      tags:
        - Pix
      summary: Pix-out transfer
      operationId: post-pix-out-transfer
      description: >
        Perform a Pix-out transfer.


        If you are using a key or QR Code, you need to call  <a
        href="https://developers.pismo.io/pismo-docs/reference/post-v2-pix-keys-validate">Validate
        DICT key</a> or <a
        href="https://developers.pismo.io/pismo-docs/reference/post-qr-code">Get
        and validate QR Code</a> before calling this endpoint. You need to enter
        the `end_to_end_id` returned from those calls here. Validation and
        transaction need to occur within 10 seconds. If you are not using a key
        or QR Code, this endpoint generates and returns an `end_to_end_id` value
        you can use in other endpoints.


        The `end_to_end_id` field is a Brazil Central Bank (BCB) requirement to
        track Pix transactions.  Basically, there are two steps to a Pix
        transaction: 1. Initialize a transaction and 2. Confirm a transaction.
        The `end_to_end_id` is sent to the BCB in both steps in order to
        identify all transaction information. 


        This endpoint generates a <a
        href="https://developers.pismo.io/events/docs/pix-pix-out-1">Pix-out
        notification received</a> event and a <a
        href="https://developers.pismo.io/events/docs/pix-financial-reconciliation-1">Pix
        reconciliation notification received</a> event.


        This endpoint also generates a <a
        href="https://developers.pismo.io/events/docs/pix-pix-in-1">Pix-in
        notification received</a> event when the funds are deposited in the
        payee's account, which can be a Pismo account or an external account.


        For all events, the `status` or `result` field indicates operation
        success or failure.


        If the request times out and the `end_to_end_id` was not returned, use
        the [Get Pix transaction by client request
        ID](ref:post-transactions-search) endpoint passing the same
        `Idempotency-Key` sent in this request as the `client_request_id` to
        recover the transaction status.


        For more information, see the [Pix instant
        payments](doc:pix-instant-payments) guide.
      parameters:
        - $ref: '#/components/parameters/AuthHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        description: Request Body
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/pix.OutTransferRequest'
      responses:
        '202':
          description: Response Body
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pix.OutTransferResponse'
        '400':
          description: Bad Request
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0020:
                  summary: SPI Gateway - API returned 4xx error
                  value:
                    code: EPIX0020
                    message: SPI Gateway - API returned 4xx error
                    details:
                      external:
                        - code: PROVIDER_ERROR_CODE
                          message: Provider error message
                EPIX0032:
                  summary: Error unmarshalling request body
                  value:
                    code: EPIX0032
                    message: Error unmarshalling request body
                EPIX0139:
                  summary: Invalid field size for Cid
                  value:
                    code: EPIX0139
                    message: Invalid field size for Cid
                EPIX0132:
                  summary: Invalid field size for Idempotency-Key
                  value:
                    code: EPIX0132
                    message: Invalid field size for Idempotency-Key
                EPIX0067:
                  summary: Requester account id does not match with body account_id
                  value:
                    code: EPIX0067
                    message: Requester account id does not match with body account_id
                EPIX0126:
                  summary: Required Key
                  value:
                    code: EPIX0126
                    message: Required Key
                EPIX0115:
                  summary: Missing required transaction identification
                  value:
                    code: EPIX0115
                    message: Missing required transaction identification
                EPIX0136:
                  summary: Missing required initiator document number
                  value:
                    code: EPIX0136
                    message: Missing required initiator document number
                EPIX0022:
                  summary: Invalid CNPJ
                  value:
                    code: EPIX0022
                    message: Invalid CNPJ
                EPIX0137:
                  summary: Invalid value for urgency field. Value must be 'HIGH'
                  value:
                    code: EPIX0137
                    message: Invalid value for urgency field. Value must be 'HIGH'
                EPIX0138:
                  summary: Invalid value for transaction type. Value must be 'TRANSFER'
                  value:
                    code: EPIX0138
                    message: >-
                      Invalid value for transaction type. Value must be
                      'TRANSFER'
                EPIX0113:
                  summary: Invalid initiation type
                  value:
                    code: EPIX0113
                    message: Invalid initiation type
                EPIX0123:
                  summary: Required withdrawal
                  value:
                    code: EPIX0123
                    message: Required withdrawal
                EPIX0120:
                  summary: Missing required purchase amount
                  value:
                    code: EPIX0120
                    message: Missing required purchase amount
                EPIX0124:
                  summary: Invalid amount
                  value:
                    code: EPIX0124
                    message: Invalid amount
                EPIX0122:
                  summary: Invalid withdraw amount
                  value:
                    code: EPIX0122
                    message: Invalid withdraw amount
                EPIX0141:
                  summary: It's not allow transaction to the same AccountID
                  value:
                    code: EPIX0141
                    message: It's not allow transaction to the same AccountID
                EPIX0019:
                  summary: Accounts - API returned 4xx error
                  value:
                    code: EPIX0019
                    message: Accounts - API returned 4xx error
                EPIX0008:
                  summary: Account blocked or cancelled
                  value:
                    code: EPIX0008
                    message: Account blocked or cancelled
                EPIX0053:
                  summary: IntlBankAccounts - API returned 4xx error
                  value:
                    code: EPIX0053
                    message: IntlBankAccounts - API returned 4xx error
                EPIX0003:
                  summary: Account not found
                  value:
                    code: EPIX0003
                    message: Account not found
                EPIX0149:
                  summary: Beneficiary is not enabled
                  value:
                    code: EPIX0113
                    message: Beneficiary is not enabled
                EPIX0114:
                  summary: Beneficiary information does not match Payee's information
                  value:
                    code: EPIX0114
                    message: Beneficiary information does not match Payee's information
                EPIX0061:
                  summary: PixDict - API returned 4xx error
                  value:
                    code: EPIX0061
                    message: PixDict - API returned 4xx error
                EPIX0068:
                  summary: Invalid EndToEndID
                  value:
                    code: EPIX0068
                    message: Invalid EndToEndID
                EPIX0130:
                  summary: Transaction type not found
                  value:
                    code: EPIX0130
                    message: Transaction type not found
                EPIX0045:
                  summary: P2P API returned 4xx error
                  value:
                    code: EPIX0045
                    message: P2P API returned 4xx error
                EPIX0110:
                  summary: The amount passed is greater than the current balance
                  value:
                    code: EPIX0110
                    message: The amount passed is greater than the current balance
                EPIX0146:
                  summary: No Orgs operation found
                  value:
                    code: EPIX0146
                    message: No Orgs operation found
                EPIX0160:
                  summary: >-
                    Field transaction_identification can not be filled when
                    initiation_type is MANUAL or DICT
                  value:
                    code: EPIX0160
                    message: >-
                      Field transaction_identification can not be filled when
                      initiation_type is MANUAL or DICT
                EPIX0049:
                  summary: Payload validation
                  value:
                    code: EPIX0049
                    message: Validation error
                    details:
                      payload:
                        - field: account_id
                          message: Field "account_id" is required
                        - field: ispb
                          message: Field "ispb" is required
                        - field: branch
                          message: Field "branch" is required
        '403':
          description: Forbidden
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0009:
                  summary: 'Missing required header: "x-tenant"'
                  value:
                    code: EPIX0009
                    message: 'Missing required header: "x-tenant"'
                EPIX0069:
                  summary: 'Missing required header: "Idempotency-Key"'
                  value:
                    code: EPIX0069
                    message: 'Missing required header: "Idempotency-Key"'
                EPIX0010:
                  summary: 'Invalid account id header: "x-account-id"'
                  value:
                    code: EPIX0010
                    message: 'Invalid account id header: "x-account-id"'
                EPIX0111:
                  summary: Anti fraud API error
                  value:
                    code: EPIX0111
                    message: Anti fraud API error
                EPIX0112:
                  summary: Rejected by Anti fraud API
                  value:
                    code: EPIX0112
                    message: Rejected by Anti fraud API
        '404':
          description: Not Found
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0051:
                  summary: IntlBankAccounts - Not found
                  value:
                    code: EPIX0051
                    message: IntlBankAccounts - Not found
                EPIX0041:
                  summary: SPI Gateway - Not found error. Check details
                  value:
                    code: EPIX0041
                    message: SPI Gateway - Not found error. Check details
        '409':
          description: Conflict
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0071:
                  summary: Pix transaction already exists
                  value:
                    code: EPIX0071
                    message: Pix transaction already exists
                EPIX0142:
                  summary: Already exist a transaction with this EndToEnd
                  value:
                    code: EPIX0142
                    message: Already exist a transaction with this EndToEnd
                EPIX0144:
                  summary: Error recording the idempotency
                  value:
                    code: EPIX0144
                    message: Error recording the idempotency
                EPIX0085:
                  summary: Duplicated CorrelationId
                  value:
                    code: EPIX0085
                    message: Duplicated CorrelationId
        '429':
          description: Too Many Requests
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0031:
                  summary: SPI Gateway - API returned a rate limit error
                  value:
                    code: EPIX0031
                    message: SPI Gateway - API returned a rate limit error
        '500':
          description: Internal Server Error
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0004:
                  summary: Internal error
                  value:
                    code: EPIX0004
                    message: Internal error
                EPIX0005:
                  summary: Error unmarshalling object
                  value:
                    code: EPIX0005
                    message: Error unmarshalling object
                EPIX0007:
                  summary: HTTP error
                  value:
                    code: EPIX0007
                    message: HTTP error
                EPIX0042:
                  summary: Accounts API timeout error
                  value:
                    code: EPIX0042
                    message: Accounts API timeout error
                EPIX0017:
                  summary: Accounts - API returned 5xx error
                  value:
                    code: EPIX0017
                    message: Accounts - API returned 5xx error
                EPIX0108:
                  summary: Fail to find banking configs
                  value:
                    code: EPIX0108
                    message: Fail to find banking configs
                EPIX0109:
                  summary: Invalid type for banking config parameter value
                  value:
                    code: EPIX0109
                    message: Invalid type for banking config parameter value
                EPIX0014:
                  summary: Database internal Error
                  value:
                    code: EPIX0014
                    message: Database internal Error
                EPIX0043:
                  summary: P2P API timeout error
                  value:
                    code: EPIX0043
                    message: P2P API timeout error
                EPIX0089:
                  summary: P2P returned invalid event date
                  value:
                    code: EPIX0089
                    message: P2P returned invalid event date
                EPIX0046:
                  summary: P2P API returned 5xx error
                  value:
                    code: EPIX0046
                    message: P2P API returned 5xx error
                EPIX0070:
                  summary: Provider returned transaction with error status
                  value:
                    code: EPIX0070
                    message: Provider returned transaction with error status
        '502':
          description: Bad Gateway
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0018:
                  summary: SPI Gateway - API returned error
                  value:
                    code: EPIX0018
                    message: SPI Gateway - API returned error
                EPIX0051:
                  summary: IntlBankAccounts - API returned 5xx error
                  value:
                    code: EPIX0051
                    message: IntlBankAccounts - API returned 5xx error
                EPIX0060:
                  summary: PixDict - API returned 5xx error
                  value:
                    code: EPIX0060
                    message: PixDict - API returned 5xx error
        '504':
          description: Gateway Timeout
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0030:
                  summary: SPI Gateway - API returned a timeout error
                  value:
                    code: EPIX0030
                    message: SPI Gateway - API returned a timeout error
                EPIX0050:
                  summary: IntlBankAccounts - API timeout
                  value:
                    code: EPIX0050
                    message: IntlBankAccounts - API timeout
                EPIX0058:
                  summary: PixDict - API timeout
                  value:
                    code: EPIX0058
                    message: PixDict - API timeout
      x-codegen-request-body-name: RequestBody
  /pix/v1/pix/out/schedule:
    post:
      tags:
        - Scheduled transfers
      summary: Schedule transfer [deprecated]
      deprecated: true
      description: >
        This endpoint is `DEPRECATED`. It is recommended you call [Schedule
        Transfer(s) V2](ref:post-schedule-pix-payment) instead.


        Schedule a Pix-out transfer for a future date.


        As with the [Pix-out transfer](ref:post-pix-out-transfer) endpoint, if
        you are using a key or QR Code, you need to call  <a
        href="https://developers.pismo.io/pismo-docs/reference/post-v2-pix-keys-validate">Validate
        DICT key</a> or <a
        href="https://developers.pismo.io/pismo-docs/reference/post-qr-code">Get
        and validate QR Code</a> before calling this endpoint. You need to enter
        the `end_to_end_id` returned from those calls here. If you are not using
        a key or QR Code, this endpoint generates and returns an `end_to_end_id`
        value you can use in other endpoints.


        The `end_to_end_id` field is a Brazil Central Bank (BCB) requirement to
        track Pix transactions.  Basically, there are two steps to a Pix
        transaction: 1. Initialize a transaction and 2. Confirm a transaction.
        The `end_to_end_id` is sent to the BCB in both steps in order to
        identify all transaction information. 


        When the transfer takes place, <a
        href="https://developers.pismo.io/events/docs/pix-pix-out-1">Pix out
        notification received</a> and <a
        href="https://developers.pismo.io/events/docs/pix-financial-reconciliation-1">Pix
        reconciliation notification received </a> events are generated.


        Also, when the transfer takes place, a <a
        href="https://developers.pismo.io/events/docs/pix-pix-in-1">Pix-in
        notification received</a> event is generated when the funds are
        deposited in the payee's account, which can be a Pismo account or an
        external account.


        For all events, the `status` or `result` field indicates operation
        success or failure.


        For more information, see the [Pix instant
        payments](doc:pix-instant-payments) guide.
      operationId: post-schedule
      parameters:
        - $ref: '#/components/parameters/AuthHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        description: Request Body
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/pix.PixOutScheduleRequest'
      responses:
        '201':
          description: Created
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pix.PixOutScheduleResponse'
        '400':
          description: Bad Request
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0003:
                  summary: Account not found
                  value:
                    code: EPIX0003
                    message: Account not found
                EPIX0019:
                  summary: Accounts - API returned 4xx error
                  value:
                    code: EPIX0019
                    message: Accounts - API returned 4xx error
                EPIX0020:
                  summary: SPI Gateway - API returned 4xx error
                  value:
                    code: EPIX0020
                    message: SPI Gateway - API returned 4xx error
                EPIX0032:
                  summary: Error unmarshalling request body
                  value:
                    code: EPIX0032
                    message: Error unmarshalling request body
                EPIX0049:
                  summary: Payload validation
                  value:
                    code: EPIX0049
                    message: Validation error
                    details:
                      payload:
                        - field: schedule_datetime
                          message: Field "schedule_datetime" is required
                        - field: initiation_type
                          message: Field "initiation_type" is required
                EPIX0091:
                  summary: Pix Schedule - schedule date is invalid
                  value:
                    code: EPIX0091
                    message: Pix Schedule - schedule date is invalid
                EPIX0092:
                  summary: Pix Schedule - EndToEndId should be provided
                  value:
                    code: EPIX0092
                    message: Pix Schedule - EndToEndId should be provided
                EPIX0099:
                  summary: Account id header does not match request body
                  value:
                    code: EPIX0099
                    message: Account id header does not match request body
                EPIX0125:
                  summary: Key cannot be sent when `initiation_type` is `MANUAL`
                  value:
                    code: EPIX0125
                    message: Key cannot be sent when `initiation_type` is `MANUAL`
                EPIX0126:
                  summary: Required Key
                  value:
                    code: EPIX0126
                    message: Required Key
        '403':
          description: Forbidden
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0009:
                  summary: 'Missing required header: "x-tenant"'
                  value:
                    code: EPIX0009
                    message: 'Missing required header: "x-tenant"'
                EPIX0010:
                  summary: 'Invalid account id header: "x-account-id"'
                  value:
                    code: EPIX0010
                    message: 'Invalid account id header: "x-account-id"'
                EPIX0069:
                  summary: 'Missing required header: "Idempotency-Key"'
                  value:
                    code: EPIX0069
                    message: 'Missing required header: "Idempotency-Key"'
                EPIX0132:
                  summary: Invalid field size for Idempotency-Key
                  value:
                    code: EPIX0132
                    message: Invalid field size for Idempotency-Key
        '404':
          description: Not Found
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0041:
                  summary: SPI Gateway - Not found error. Check details
                  value:
                    code: EPIX0041
                    message: SPI Gateway - Not found error. Check details
        '409':
          description: Conflict
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0008:
                  summary: Account blocked or cancelled
                  value:
                    code: EPIX0008
                    message: Account blocked or cancelled
        '429':
          description: Too Many Requests
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0031:
                  summary: SPI Gateway - API returned a rate limit error
                  value:
                    code: EPIX0031
                    message: SPI Gateway - API returned a rate limit error
        '500':
          description: Internal Server Error
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0004:
                  summary: Internal server error
                  value:
                    code: EPIX0004
                    message: Internal server error
                EPIX0005:
                  summary: Error unmarshalling object
                  value:
                    code: EPIX0005
                    message: Error unmarshalling object
                EPIX0007:
                  summary: HTTP error
                  value:
                    code: EPIX0007
                    message: HTTP error
                EPIX0017:
                  summary: Accounts - API returned 5xx error
                  value:
                    code: EPIX0017
                    message: Accounts - API returned 5xx error
                EPIX0042:
                  summary: Accounts API timeout error
                  value:
                    code: EPIX0042
                    message: Accounts API timeout error
                EPIX0095:
                  summary: Pix Schedule - Error marshalling schedule transfer
                  value:
                    code: EPIX0095
                    message: Pix Schedule - Error marshalling schedule transfer
                EPIX0096:
                  summary: Pix Schedule - Error on start queue scheduler
                  value:
                    code: EPIX0096
                    message: Pix Schedule - Error on start queue scheduler
                EPIX0097:
                  summary: Pix Schedule - Error persisting transaction
                  value:
                    code: EPIX0097
                    message: Pix Schedule - Error persisting transaction
                EPIX0098:
                  summary: Pix Schedule - Error persisting scheduled transfers
                  value:
                    code: EPIX0098
                    message: Pix Schedule - Error persisting scheduled transfers
        '502':
          description: Bad Gateway
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0018:
                  summary: SPI Gateway - API returned error
                  value:
                    code: EPIX0018
                    message: SPI Gateway - API returned error
        '504':
          description: Gateway Timeout
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0030:
                  summary: SPI Gateway - API returned a timeout error
                  value:
                    code: EPIX0030
                    message: SPI Gateway - API returned a timeout error
      x-codegen-request-body-name: RequestBody
  /pix/v1/pix/out/accounts/{account_id}/schedules:
    get:
      tags:
        - Scheduled transfers
      summary: List account scheduled transfers
      description: Retrieve an account's scheduled transfers.
      operationId: get-schedules
      parameters:
        - $ref: '#/components/parameters/AuthHeader'
        - $ref: '#/components/parameters/AccountIDpath'
        - $ref: '#/components/parameters/PerPageQuery'
        - $ref: '#/components/parameters/lastEvaluatedCreationDatetimeQuery'
        - $ref: '#/components/parameters/ScheduleIdQuery'
        - $ref: '#/components/parameters/ScheduleCreationBeginDatetimeQuery'
        - $ref: '#/components/parameters/ScheduleCreationEndDatetimeQuery'
        - $ref: '#/components/parameters/ScheduleBeginDatetimeQuery'
        - $ref: '#/components/parameters/ScheduleEndDatetimeQuery'
      responses:
        '200':
          description: Response Body
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pix.SchedulesFindResponse'
        '400':
          description: Bad Request
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0100:
                  summary: Invalid account id in parameter
                  value:
                    code: EPIX0100
                    message: Invalid account id in parameter
                EPIX0061:
                  summary: Invalid value for query param
                  value:
                    code: EPIX0061
                    message: Invalid value for query param
                EPIX0103:
                  summary: >-
                    Invalid value for datetime query parameter. RFC3339 format
                    must be used
                  value:
                    code: EPIX0103
                    message: >-
                      Invalid value for datetime query parameter. RFC3339 format
                      must be used
        '403':
          description: Forbidden
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0009:
                  summary: 'Missing required header: "x-tenant"'
                  value:
                    code: EPIX0009
                    message: 'Missing required header: "x-tenant"'
                EPIX0010:
                  summary: 'Invalid account id header: "x-account-id"'
                  value:
                    code: EPIX0010
                    message: 'Invalid account id header: "x-account-id"'
                EPIX0102:
                  summary: Entity"s account id does not match x-account-id header
                  value:
                    code: EPIX0102
                    message: Entity"s account id does not match x-account-id header
        '404':
          description: Not Found
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0101:
                  summary: Pix Schedule - Error scheduled transfers not found
                  value:
                    code: EPIX0101
                    message: Pix Schedule - Error scheduled transfers not found
        '500':
          description: Internal Server Error
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0004:
                  summary: Internal server error
                  value:
                    code: EPIX0004
                    message: Internal server error
  /pix/v1/pix/out/schedule/{scheduler_id}:
    delete:
      tags:
        - Scheduled transfers
      summary: Cancel scheduled transfer
      description: Cancel a scheduled Pix-out transfer.
      operationId: delete-transfer
      parameters:
        - $ref: '#/components/parameters/AuthHeader'
        - $ref: '#/components/parameters/ScheduleIdPath'
      responses:
        '204':
          description: ''
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content: {}
        '403':
          description: Forbidden
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            Application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0009:
                  summary: 'Missing required header: "x-tenant"'
                  value:
                    code: EPIX0009
                    message: 'Missing required header: "x-tenant"'
                EPIX0010:
                  summary: 'Invalid account id header: "x-account-id"'
                  value:
                    code: EPIX0010
                    message: 'Invalid account id header: "x-account-id"'
                EPIX0102:
                  summary: Entity"s account id does not match x-account-id header
                  value:
                    code: EPIX0102
                    message: Entity"s account id does not match x-account-id header
        '404':
          description: Not Found
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            Application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0101:
                  summary: Pix Schedule - Error scheduled transfers not found
                  value:
                    code: EPIX0101
                    message: Pix Schedule - Error scheduled transfers not found
        '500':
          description: Internal Server Error
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            Application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0004:
                  summary: Internal server error
                  value:
                    code: EPIX0004
                    message: Internal server error
                EPIX0014:
                  summary: Database internal Error
                  value:
                    code: EPIX0014
                    message: Database internal Error
  /pix/v1/pix/in/reversal:
    post:
      summary: Pix reversal
      operationId: post-pix-reversal
      description: >
        Reverse a Pix-out transfer.


        This endpoint generates a <a
        href="https://developers.pismo.io/events/docs/pix-pix-out-reversal-1">Pix-out
        reversed</a> event and a <a
        href="https://developers.pismo.io/events/docs/pix-financial-reconciliation-1">Pix 
        reconciliation notification received</a> event.


        This endpoint also generates a <a
        href="https://developers.pismo.io/events/docs/pix-pix-in-reversal-1">Pix-in
        reversed</a> event when the funds are transferred from the payee's
        account, which can be a Pismo account or an external account.


        For all events, the `status` or `result` field indicates operation
        success or failure.


        For more information, see the [Pix instant
        payments](doc:pix-instant-payments) guide.
      tags:
        - Pix
      parameters:
        - $ref: '#/components/parameters/AuthHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
      requestBody:
        description: Request Body
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/pix.InReversalRequest'
        required: true
      responses:
        '202':
          description: Response Body
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pix.InReversalResponse'
        '400':
          description: Bad Request
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0005:
                  summary: Error unmarshalling object
                  value:
                    code: EPIX0005
                    message: Error unmarshalling object
                EPIX0020:
                  summary: SPI Gateway - API returned 4xx error
                  value:
                    code: EPIX0020
                    message: SPI Gateway - API returned 4xx error
                    details:
                      external:
                        - code: PCE001
                          message: >-
                            The field "ClientRequestId" must match the regular
                            expression [a-zA-Z0-9]{1,35}.
                EPIX0045:
                  summary: P2P API returned 4xx error
                  value:
                    code: EPIX0045
                    message: P2P API returned 4xx error
                EPIX0049:
                  summary: Payload validation
                  value:
                    code: EPIX0049
                    message: Validation error
                    details:
                      payload:
                        - field: branch
                          message: Field "branch" is required
                        - field: account_number
                          message: Field "account_number" is required
                        - field: amount
                          message: Field "amount" is required
                EPIX0084:
                  summary: >-
                    P2P API returned 400 error - Amount cannot exceed the
                    original transaction amount
                  value:
                    code: EPIX0084
                    message: >-
                      P2P API returned 400 error - Amount cannot exceed the
                      original transaction amount
        '403':
          description: Forbidden
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0009:
                  summary: 'Missing required header: "x-tenant"'
                  value:
                    code: EPIX0009
                    message: 'Missing required header: "x-tenant"'
                EPIX0010:
                  summary: 'Invalid account id header: "x-account-id"'
                  value:
                    code: EPIX0010
                    message: 'Invalid account id header: "x-account-id"'
                EPIX0069:
                  summary: 'Missing required header: "Idempotency-Key"'
                  value:
                    code: EPIX0069
                    message: 'Missing required header: "Idempotency-Key"'
                EPIX0132:
                  summary: Invalid field size for Idempotency-Key
                  value:
                    code: EPIX0132
                    message: Invalid field size for Idempotency-Key
        '404':
          description: Not Found
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0003:
                  summary: Account not found
                  value:
                    code: EPIX0003
                    message: Account not found
                EPIX0041:
                  summary: SPI Gateway - Not found error. Check details
                  value:
                    code: EPIX0041
                    message: SPI Gateway - Not found error. Check details
        '409':
          description: Conflict
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0008:
                  summary: Account blocked or cancelled
                  value:
                    code: EPIX0008
                    message: Account blocked or cancelled
                EPIX0071:
                  summary: Pix transaction already exists
                  value:
                    code: EPIX0071
                    message: Pix transaction already exists
                EPIX0083:
                  summary: >-
                    P2P API returned 409 error - Authorization was already
                    refunded/reversed
                  value:
                    code: EPIX0083
                    message: >-
                      P2P API returned 409 error - Authorization was already
                      refunded/reversed
                EPIX0085:
                  summary: P2P API returned 409 error - Duplicated CorrelationId
                  value:
                    code: EPIX0085
                    message: P2P API returned 409 error - Duplicated CorrelationId
                EPIX0086:
                  summary: Pix Transaction not found
                  value:
                    code: EPIX0086
                    message: Pix Transaction not found
        '429':
          description: Too Many Requests
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0031:
                  summary: SPI Gateway - API returned a rate limit error
                  value:
                    code: EPIX0031
                    message: SPI Gateway - API returned a rate limit error
        '500':
          description: Internal Server Error
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0004:
                  summary: Internal server error
                  value:
                    code: EPIX0004
                    message: Internal server error
                EPIX0005:
                  summary: Error unmarshalling object
                  value:
                    code: EPIX0005
                    message: Error unmarshalling object
                EPIX0007:
                  summary: HTTP error
                  value:
                    code: EPIX0007
                    message: HTTP error
                EPIX0014:
                  summary: Database internal Error
                  value:
                    code: EPIX0014
                    message: Database internal Error
                EPIX0043:
                  summary: P2P API timeout error
                  value:
                    code: EPIX0043
                    message: P2P API timeout error
        '502':
          description: Bad Gateway
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0018:
                  summary: SPI Gateway - API returned error
                  value:
                    code: EPIX0018
                    message: SPI Gateway - API returned error
        '504':
          description: Gateway Timeout
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0030:
                  summary: SPI Gateway - API returned a timeout error
                  value:
                    code: EPIX0030
                    message: SPI Gateway - API returned a timeout error
      x-codegen-request-body-name: RequestBody
  /pix/v1/pix/qrcode/validate:
    post:
      tags:
        - Pix
      summary: Validate QR Code
      description: Retrieve and validate Pix QR Code.
      operationId: post-qr-code
      parameters:
        - $ref: '#/components/parameters/AuthHeader'
      requestBody:
        description: Request Body
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/pix.QRCodeRequest'
      responses:
        '200':
          description: Response Body
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pix.QRCodeResponse'
        '400':
          description: Bad Request
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0003:
                  summary: Account not found
                  value:
                    code: EPIX0003
                    message: Account not found
                EPIX0015:
                  summary: x-account-id is not a valid number
                  value:
                    code: EPIX0015
                    message: x-account-id is not a valid number
                EPIX0019:
                  summary: Accounts - API returned 4xx error
                  value:
                    code: EPIX0019
                    message: Accounts - API returned 4xx error
                EPIX0020:
                  summary: SPI Gateway - API returned 4xx error
                  value:
                    code: EPIX0020
                    message: SPI Gateway - API returned 4xx error
                    details:
                      external:
                        - code: PCE001
                          message: >-
                            The value "000000012345" is invalid for PI-PayerId.
                            The value must match "((^[0-9]11$)|(^[0-9]14$))"
                EPIX0032:
                  summary: Error unmarshalling request body
                  value:
                    code: EPIX0032
                    message: Error unmarshalling request body
                EPIX0061:
                  summary: PixDict - API returned 4xx error
                  value:
                    code: EPIX0061
                    message: PixDict - API returned 4xx error
                EPIX0070:
                  summary: >-
                    QRCPS API do returned a merchant information without key and
                    url
                  value:
                    code: EPIX0070
                    message: >-
                      QRCPS API do returned a merchant information without key
                      and url
                EPIX0074:
                  summary: QRCPS API returned 4xx error
                  value:
                    code: EPIX0074
                    message: QRCPS API returned 4xx error
                EPIX0076:
                  summary: QRCPS API do not returned a merchant information
                  value:
                    code: EPIX0076
                    message: QRCPS API do not returned a merchant information
                EPIX0127:
                  summary: Invalid field size for merchant category code
                  value:
                    code: EPIX0127
                    message: Invalid field size for merchant category code
                EPIX0128:
                  summary: >-
                    Invalid value for transaction currency. Local currency must
                    be used
                  value:
                    code: EPIX0128
                    message: >-
                      Invalid value for transaction currency. Local currency
                      must be used
                EPIX0129:
                  summary: >-
                    Invalid value for transaction amount. Transaction amount
                    must be greater than zero
                  value:
                    code: EPIX0129
                    message: >-
                      Invalid value for transaction amount. Transaction amount
                      must be greater than zero
                EPIX0131:
                  summary: Invalid QR Code, the payload format indicator is invalid
                  value:
                    code: EPIX0131
                    message: Invalid QR Code, the payload format indicator is invalid
                EPIX0134:
                  summary: >-
                    Invalid QR Code, a merchant information cannot contain key
                    and url filled in
                  value:
                    code: EPIX0134
                    message: >-
                      Invalid QR Code, a merchant information cannot contain key
                      and url filled in
                EPIX0143:
                  summary: Invalid QR Code, the country code must be BR
                  value:
                    code: EPIX0143
                    message: Invalid country code on QR Code. Must be `BR`
        '403':
          description: Forbidden
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0009:
                  summary: 'Missing required header: "x-tenant"'
                  value:
                    code: EPIX0009
                    message: 'Missing required header: "x-tenant"'
                EPIX0010:
                  summary: 'Invalid account id header: "x-account-id"'
                  value:
                    code: EPIX0010
                    message: 'Invalid account id header: "x-account-id"'
        '404':
          description: Not Found
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0041:
                  summary: SPI Gateway - Not found error. Check details
                  value:
                    code: EPIX0041
                    message: SPI Gateway - Not found error. Check details
                EPIX0059:
                  summary: PixDict - Not found
                  value:
                    code: EPIX0059
                    message: PixDict - Not found
        '409':
          description: Conflict
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0008:
                  summary: Account blocked or cancelled
                  value:
                    code: EPIX0008
                    message: Account blocked or cancelled
        '429':
          description: Too Many Requests
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0031:
                  summary: SPI Gateway - API returned a rate limit error
                  value:
                    code: EPIX0031
                    message: SPI Gateway - API returned a rate limit error
        '500':
          description: Internal Server Error
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0004:
                  summary: Internal server error
                  value:
                    code: EPIX0004
                    message: Internal server error
                EPIX0005:
                  summary: Error unmarshalling object
                  value:
                    code: EPIX0005
                    message: Error unmarshalling object
                EPIX0007:
                  summary: HTTP error
                  value:
                    code: EPIX0007
                    message: HTTP error
                EPIX0017:
                  summary: Accounts - API returned 5xx error
                  value:
                    code: EPIX0017
                    message: Accounts - API returned 5xx error
                EPIX0042:
                  summary: Accounts API timeout error
                  value:
                    code: EPIX0042
                    message: Accounts API timeout error
        '502':
          description: Bad Gateway
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0018:
                  summary: SPI Gateway - API returned error
                  value:
                    code: EPIX0018
                    message: SPI Gateway - API returned error
                EPIX0060:
                  summary: PixDict - API returned 5xx error
                  value:
                    code: EPIX0060
                    message: PixDict - API returned 5xx error
                EPIX0075:
                  summary: QRCPS API returned 5xx error
                  value:
                    code: EPIX0075
                    message: QRCPS API returned 5xx error
        '504':
          description: Gateway Timeout
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0073:
                  summary: QRCPS API timeout error
                  value:
                    code: EPIX0073
                    message: QRCPS API timeout error
                EPIX0030:
                  summary: SPI Gateway - API returned a timeout error
                  value:
                    code: EPIX0030
                    message: SPI Gateway - API returned a timeout error
                EPIX0058:
                  summary: PixDict - API timeout
                  value:
                    code: EPIX0058
                    message: PixDict - API timeout
      x-codegen-request-body-name: RequestBody
  /pix/v1/pix/receipts:
    get:
      tags:
        - Pix
      summary: List account receipts
      description: >
        Retrieve account's receipts with filter options. The account is parsed
        from the account authorization token.
      operationId: get-receipts
      parameters:
        - $ref: '#/components/parameters/AuthHeader'
        - $ref: '#/components/parameters/PerPageQuery'
        - $ref: '#/components/parameters/BeginDateQuery'
        - $ref: '#/components/parameters/EndDateQuery'
        - $ref: '#/components/parameters/TransactionTypeQuery'
        - $ref: '#/components/parameters/EndToEndIDquery'
        - $ref: '#/components/parameters/ReversalEndToEndIDquery'
        - $ref: '#/components/parameters/OrderQuery'
        - $ref: '#/components/parameters/AfterIdQuery'
      responses:
        '200':
          description: Response Body
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pix.ReceiptFindResponse'
        '400':
          description: Bad Request
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0061:
                  summary: Invalid value for query param
                  value:
                    code: EPIX0061
                    message: Invalid value for query param
                EPIX0062:
                  summary: >-
                    Invalid value for query param. Date must be RFC3339 format,
                    i.e., "2023-04-12T23:20:50.52Z"
                  value:
                    code: EPIX0062
                    message: Invalid value for query param. Date must be RFC3339 format
                EPIX0063:
                  summary: Invalid value for query param. Date must be RFC3339 format
                  value:
                    code: EPIX0063
                    message: Invalid value for query param. Date must be RFC3339 format
                EPIX0065:
                  summary: '? must be greater than ?'
                  value:
                    code: EPIX0065
                    message: '? must be greater than ?'
                EPIX0077:
                  summary: >-
                    query parameter is invalid. Should use the last value of
                    "last_id" field
                  value:
                    code: EPIX0077
                    message: >-
                      query parameter is invalid. Should use the last value of
                      "last_id" field
        '403':
          description: Forbidden
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0009:
                  summary: 'Missing required header: "x-tenant"'
                  value:
                    code: EPIX0009
                    message: 'Missing required header: "x-tenant"'
                EPIX0010:
                  summary: 'Invalid account id header: "x-account-id"'
                  value:
                    code: EPIX0010
                    message: 'Invalid account id header: "x-account-id"'
        '404':
          description: Not Found
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0135:
                  summary: Receipt not found
                  value:
                    code: EPIX0135
                    message: Receipt not found
        '500':
          description: Internal Server Error
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0004:
                  summary: Internal server error
                  value:
                    code: EPIX0004
                    message: Internal server error
  /pix/v1/pix/end-to-end/{end_to_end_id}/transaction:
    get:
      tags:
        - Pix
      summary: Get Pix transaction by end-to-end ID
      description: >
        Retrieve transaction information given an `end-to-end` value in the
        path.


        **Note:** This endpoint takes an account token - an access token encoded
        with a Pismo accound ID.
      operationId: get-transaction-e2e
      parameters:
        - $ref: '#/components/parameters/AuthHeader'
        - $ref: '#/components/parameters/EndToEndIDpath'
      responses:
        '200':
          description: Response Body
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pix.TransactionWithDetailResponse'
        '403':
          description: Forbidden
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0009:
                  summary: 'Missing required header: "x-tenant"'
                  value:
                    code: EPIX0009
                    message: 'Missing required header: "x-tenant"'
                EPIX0010:
                  summary: 'Invalid account id header: "x-account-id"'
                  value:
                    code: EPIX0010
                    message: 'Invalid account id header: "x-account-id"'
        '404':
          description: Bad Request
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0086:
                  summary: Transaction not found.
                  value:
                    code: EPIX0086
                    message: Transaction not found.
                EPIX0087:
                  summary: Pix TransactionDetail not found.
                  value:
                    code: EPIX0087
                    message: Pix TransactionDetail not found.
        '500':
          description: Internal Server Error
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0004:
                  summary: Internal server error
                  value:
                    code: EPIX0004
                    message: Internal server error
  /pix/v1/pix/transactions/search:
    post:
      summary: Get Pix transaction by client request ID
      description: >
        Get a Pix transaction using the `client_request_id` passed when the
        transaction was created.


        The `client_request_id` is the value passed in the `Idempotency-Key`
        header when creating a cashin (Pix-in) or cashout (Pix-out)
        transaction. 


        This endpoint is useful, for example, when a [Pix-out
        transfer](ref:post-pix-out-transfer) request times out and the client
        does not yet have the `end_to_end_id` needed to query the transaction
        through the [Get Pix transaction by end-to-end
        ID](ref:get-transaction-e2e) endpoint.


        **Note:** This endpoint takes an account token - an access token encoded
        with a Pismo account ID.
      operationId: post-transactions-search
      tags:
        - Pix
      parameters:
        - $ref: '#/components/parameters/AuthHeader'
      requestBody:
        description: Request Body
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/pix.TransactionSearchRequest'
      responses:
        '200':
          description: Response Body
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pix.TransactionWithDetailResponse'
        '400':
          description: Bad Request
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0032:
                  summary: Error unmarshalling request body
                  value:
                    code: EPIX0032
                    message: Error unmarshalling request body
                EPIX0049:
                  summary: Payload validation
                  value:
                    code: EPIX0049
                    message: Validation error
                    details:
                      payload:
                        - field: client_request_id
                          message: Field "client_request_id" is required
        '403':
          description: Forbidden
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0009:
                  summary: 'Missing required header: "x-tenant"'
                  value:
                    code: EPIX0009
                    message: 'Missing required header: "x-tenant"'
                EPIX0010:
                  summary: 'Invalid account id header: "x-account-id"'
                  value:
                    code: EPIX0010
                    message: 'Invalid account id header: "x-account-id"'
        '404':
          description: Not Found
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0086:
                  summary: Pix Transaction not found
                  value:
                    code: EPIX0086
                    message: Pix Transaction not found
                EPIX0087:
                  summary: Pix TransactionDetail is not found
                  value:
                    code: EPIX0087
                    message: Pix TransactionDetail is not found
        '500':
          description: Internal Server Error
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/customer.Error'
              examples:
                EPIX0004:
                  summary: Internal server error
                  value:
                    code: EPIX0004
                    message: Internal server error
                EPIX0014:
                  summary: Database internal Error
                  value:
                    code: EPIX0014
                    message: Database internal Error
      x-codegen-request-body-name: RequestBody
  /pix/v1/pix/transmitter-withdrawal/validate:
    post:
      summary: Validate bank as withdrawal servicer
      description: >-
        Validate if a participant (bank) is a withdrawal service facilitator or
        not.
      operationId: post-validate
      tags:
        - Withdrawal services
      parameters:
        - $ref: '#/components/parameters/CIDheader'
        - $ref: '#/components/parameters/ISPBquery'
      responses:
        '200':
          description: Response Body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pix.ValidateTransmitterWithdrawalResponse'
        '400':
          $ref: '#/components/responses/400BadRequest2'
        '403':
          $ref: '#/components/responses/403Forbidden2'
        '404':
          $ref: '#/components/responses/404NotFound2'
        '429':
          $ref: '#/components/responses/429TooManyRequests2'
        '500':
          $ref: '#/components/responses/500InternalServer2'
        '502':
          $ref: '#/components/responses/502BadGateway2'
        '504':
          $ref: '#/components/responses/504GatewayTimeout2'
  /pix/v1/pix/withdrawal-service-point:
    get:
      summary: Get withdrawal service agent information
      description: >
        Get information about withdrawal service agents (WSA) or the direct ISP
        participant providing the withdrawal service. 


        A WSA is a service provider that facilitates the withdrawal of funds in
        Pix. It acts as a bridge between Pix and customers who want to withdraw
        funds from their accounts.


        A WSA point is a location or access point where users can physically or
        digitally access WSA services. This could be a physical location such as
        a bank branch or retail store or digital acceess point such as a mobile
        app or web portal.
      operationId: get-withdrawal-service-point
      tags:
        - Withdrawal services
      parameters:
        - $ref: '#/components/parameters/CIDheader'
        - $ref: '#/components/parameters/ISPBtransmitterQuery'
        - $ref: '#/components/parameters/PageQuery'
        - $ref: '#/components/parameters/limitQuery'
      responses:
        '200':
          description: Response Body
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pix.WithdrawalServicePointResponse'
        '400':
          $ref: '#/components/responses/400BadRequest2'
        '403':
          $ref: '#/components/responses/403Forbidden2'
        '404':
          $ref: '#/components/responses/404NotFound2'
        '429':
          $ref: '#/components/responses/429TooManyRequests2'
        '500':
          $ref: '#/components/responses/500InternalServer2'
        '502':
          $ref: '#/components/responses/502BadGateway2'
        '504':
          $ref: '#/components/responses/504GatewayTimeout2'
  /pix/v2/pix/out/scheduler:
    post:
      summary: Schedule transfer(s) V2
      description: >
        Schedule a Pix-out transfer or Pix-out recurring transfers. 


        This endpoint gives you more scheduling options than the version 1
        [Schedule
        transfer](https://developers.pismo.io/pismo-docs/reference/post-schedule)
        endpoint. These include additional options (`scheduler_details` object)
        for recurring transfers.


        As with the [Pix-out transfer](ref:post-pix-out-transfer) endpoint, if
        you are using a key or QR Code, you need to call  <a
        href="https://developers.pismo.io/pismo-docs/reference/post-v2-pix-keys-validate">Validate
        DICT key</a> or <a
        href="https://developers.pismo.io/pismo-docs/reference/post-qr-code">Get
        and validate QR Code</a> before calling this endpoint. You need to enter
        the `end_to_end_id` returned from those calls here. If you are not using
        a key or QR Code, this endpoint generates and returns an `end_to_end_id`
        value you can use in other endpoints.


        The `end_to_end_id` field is a Brazil Central Bank (BCB) requirement to
        track Pix transactions.  Basically, there are two steps to a Pix
        transaction: 1. Initialize a transaction and 2. Confirm a transaction.
        The `end_to_end_id` is sent to the BCB in both steps in order to
        identify all transaction information.


        This endpoint directly generates an [Instant pay scheduler status
        changed](https://developers.pismo.io/events/docs/instantpay-scheduler-scheduler-status-change-1)
        event.


        To create a recurring payment, set `type` = `RECURRING` and pass a value
        for `cycle`. This property has 2 attributes and should be set as shown
        in the sample table below: 


        `cycle.type` | `cycle.count` | New transaction created every:

        ---------- | ----------- | ------------ 

        `DAILY`      | 5           | 5 days

        `WEEKLY`     | 2           | 2 weeks

        `MONTHLY`    | 1           | 1 month

        `MONTHLY`    | 2           | 2 months

        `YEARLY`     | 1           | 1 year


        **Notes**


        * `WEEKLY` sets the `start_date` weekday. For a scheduler with
        `start_date` = Thursday, the next transaction is always on a Thursday.


        * `MONTHLY / YEARLY` sets the `start_date` day. For a scheduler with
        `start_date` = `2024-10-12`, the next transaction is always created on
        day 12 of each cycle count. Now, for days `29`, `30` and `31`, the
        scheduler uses the previous month's last day when that day doesn't exist
        in the current month. For example, considering a scheduler starting at
        `2024-01-30`, the next transaction is created on `2024-02-29`.


        **Events**


        * When the transfer takes place, <a
        href="https://developers.pismo.io/events/docs/pix-pix-out-1">Pix-out
        notification received</a> and <a
        href="https://developers.pismo.io/events/docs/pix-financial-reconciliation-1">Pix
        reconciliation notification received </a> events are generated.


        * Also, when the transfer takes place, a <a
        href="https://developers.pismo.io/events/docs/pix-pix-in-1">Pix-in
        notification received</a> event is generated when the funds are
        deposited in the payee's account, which can be a Pismo account or an
        external account.


        * For all events, the `status` or `result` field indicates operation
        success or failure.


        For more information, refer to the [Pix instant
        payments](doc:pix-instant-payments) guide.


        **Note:** This endpoint takes an account token - an access token encoded
        with a Pismo accound ID.
      operationId: post-schedule-pix-payment
      tags:
        - Scheduled transfers V2
      parameters:
        - $ref: '#/components/parameters/AuthHeader'
        - $ref: '#/components/parameters/IdempotencyKeyHeader'
        - $ref: '#/components/parameters/CIDheader'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/scheduler.SchedulerRegisterRequest'
      responses:
        '201':
          description: Created
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/scheduler.SchedulerRegisterResponse'
        '400':
          $ref: '#/components/responses/400BadRequest'
        '403':
          $ref: '#/components/responses/403Forbidden'
        '404':
          $ref: '#/components/responses/404NotFound'
        '409':
          $ref: '#/components/responses/409Conflict'
        '429':
          $ref: '#/components/responses/429TooManyRequests'
        '500':
          $ref: '#/components/responses/500InternalServer'
        '502':
          $ref: '#/components/responses/502BadGateway'
        '504':
          $ref: '#/components/responses/504GatewayTimeout'
    get:
      summary: List scheduled Pix transfers
      description: List scheduled Pix transfers with filtering options.
      operationId: list-scheduled-transfers
      tags:
        - Scheduled transfers V2
      parameters:
        - $ref: '#/components/parameters/AuthHeader'
        - $ref: '#/components/parameters/CIDheader'
        - $ref: '#/components/parameters/BeginDateQuery'
        - $ref: '#/components/parameters/EndDateQuery'
        - $ref: '#/components/parameters/SchedulerStatusQuery'
        - $ref: '#/components/parameters/SchedulerTypeQuery'
        - $ref: '#/components/parameters/SchedulerIntervalQuery'
        - $ref: '#/components/parameters/PerPageQuery'
        - $ref: '#/components/parameters/AfterIdQuery'
      responses:
        '200':
          description: Response Body
          headers:
            x-cid:
              schema:
                type: string
              description: Request tracking ID.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/scheduler.SchedulerSearchQueryResponse2'
        '400':
          $ref: '#/components/responses/400BadRequest'
        '403':
          $ref: '#/components/responses/403Forbidden'
        '404':
          $ref: '#/components/responses/404NotFound'
        '500':
          $ref: '#/components/responses/500InternalServer'
  /pix/v2/pix/out/scheduler/{scheduler_id}/execution/{execution_id}/advance:
    post:
      summary: Execute scheduled Pix-out transfer in advance
      description: >-
        Perform a scheduled Pix-out transfer execution in advance. A scheduled
        transfer can have many executions if, for example, it is recurring. 


        This endpoint generates a <a
        href="https://developers.pismo.io/events/docs/instantpay-scheduler-execution-status-change-1">Instant
        pay execution status changed</a> event.
      operationId: advance-pix-out-execution
      parameters:
        - $ref: '#/components/parameters/AuthHeader'
        - $ref: '#/components/parameters/ScheduleIdPath'
        - $ref: '#/components/parameters/ExecutionIDpath'
      tags:
        - Scheduled transfers V2
      responses:
        '202':
          description: Success
          headers:
            x-cid:
              description: Request tracking identifier.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: string
        '403':
          $ref: '#/components/responses/403Forbidden'
        '404':
          $ref: '#/components/responses/404NotFound'
        '500':
          $ref: '#/components/responses/500InternalServer'
  /pix/v2/pix/out/scheduler/{scheduler_id}:
    get:
      summary: Get scheduled transfer
      description: >
        Get a scheduled Pix payment.


        **Note:** This endpoint takes an account token - an access token encoded
        with a Pismo accound ID.
      operationId: get-scheduled-pix-payment
      tags:
        - Scheduled transfers V2
      parameters:
        - $ref: '#/components/parameters/AuthHeader'
        - $ref: '#/components/parameters/ScheduleIdPath'
        - $ref: '#/components/parameters/CIDheader'
        - $ref: '#/components/parameters/ShowPreviewQuery'
      responses:
        '200':
          description: Response Body
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/scheduler.SchedulerSearchResponse'
        '400':
          $ref: '#/components/responses/400BadRequest'
        '403':
          $ref: '#/components/responses/403Forbidden'
        '404':
          $ref: '#/components/responses/404NotFound'
        '500':
          $ref: '#/components/responses/500InternalServer'
    delete:
      summary: Cancel scheduled transfer V2
      description: >
        Cancel scheduled Pix transfer.


        This endpoint generates a <a
        href="https://developers.pismo.io/events/docs/instantpay-scheduler-scheduler-status-change-1">Instant
        pay scheduler status change</a> event.
      operationId: delete-cancel-pix-out
      tags:
        - Scheduled transfers V2
      parameters:
        - $ref: '#/components/parameters/AuthHeader'
        - $ref: '#/components/parameters/ScheduleIdPath'
        - $ref: '#/components/parameters/CIDheader'
      responses:
        '204':
          description: ''
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
        '403':
          $ref: '#/components/responses/403Forbidden'
        '404':
          $ref: '#/components/responses/404NotFound'
        '500':
          $ref: '#/components/responses/500InternalServer'
  /pix/v2/pix/out/scheduler/:scheduler_id/executions:
    get:
      summary: List Pix scheduler executions
      description: >
        List Pix payment scheduler executions - scheduled Pix transfers that
        have run to completion. A scheduled transfer can have many executions
        if, for example, it is recurring.
      tags:
        - Scheduled transfers V2
      operationId: get-scheduler-executions
      parameters:
        - $ref: '#/components/parameters/AuthHeader'
        - $ref: '#/components/parameters/CIDheader'
        - $ref: '#/components/parameters/BeginDateQuery2'
        - $ref: '#/components/parameters/EndDateQuery2'
        - $ref: '#/components/parameters/ExecutionStatusQuery'
        - $ref: '#/components/parameters/PerPageQuery'
        - $ref: '#/components/parameters/AfterIdQuery'
      responses:
        '200':
          description: Response Body
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/scheduler.ExecutionsListResponse'
        '400':
          $ref: '#/components/responses/400BadRequest'
        '403':
          $ref: '#/components/responses/403Forbidden'
        '404':
          $ref: '#/components/responses/404NotFound'
        '500':
          $ref: '#/components/responses/500InternalServer'
  /pix/v2/pix/out/scheduler/{scheduler_id}/execution/{execution_id}:
    delete:
      summary: Cancel Pix-out scheduled execution
      description: >-
        Perform a Pix-out execution cancel. A scheduled transfer can have many
        executions if, for example, it is recurring. 


        This endpoint generates a <a
        href="https://developers.pismo.io/events/docs/instantpay-scheduler-execution-status-change-1">Instant
        pay execution status changed</a> event.


        **Note:** This endpoint takes an account token - an access token encoded
        with a Pismo accound ID.
      operationId: cancel-pix-out-execution
      parameters:
        - $ref: '#/components/parameters/AuthHeader'
        - $ref: '#/components/parameters/ScheduleIdPath'
        - $ref: '#/components/parameters/ExecutionIDpath'
      tags:
        - Scheduled transfers V2
      responses:
        '204':
          description: Success
          headers:
            x-cid:
              description: Request tracking identifier.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: string
        '403':
          $ref: '#/components/responses/403Forbidden'
        '404':
          $ref: '#/components/responses/404NotFound'
        '500':
          $ref: '#/components/responses/500InternalServer'
  /pix/v1/pix/provider/transaction/type/{type}/end-to-end/{end_to_end_id}:
    get:
      summary: Get transaction from provider
      description: >
        Get Pix transaction from the provider given its type and end-to-end-ID
        in the path. Can be used to verify that both provider and Pismo have the
        same information. 


        **Note:** This endpoint takes an account token - an access token encoded
        with a Pismo accound ID.
      operationId: get-pix-transaction
      tags:
        - Pix
      parameters:
        - $ref: '#/components/parameters/AuthHeader'
        - $ref: '#/components/parameters/TransactionTypePath'
        - $ref: '#/components/parameters/EndToEndIDpath'
      responses:
        '200':
          description: OK
          headers:
            x-cid:
              description: Request tracking ID.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pix.GetTransactionByTypeAndE2EResponse'
        '400':
          $ref: '#/components/responses/400BadRequest'
        '403':
          $ref: '#/components/responses/403Forbidden'
        '404':
          $ref: '#/components/responses/404NotFound'
        '500':
          $ref: '#/components/responses/500InternalServer'
