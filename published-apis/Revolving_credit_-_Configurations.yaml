openapi: 3.1.0
info:
  title: Revolving credit - Configurations
  version: 2.74.0
  description: Endpoints for setting up the revolving credit product
  contact:
    name: API Support
    url: https://developers.pismo.io/support/
  license:
    name: Copyright Pismo
    identifier: Pismo
servers:
  - url: https://sandbox.pismolabs.io/credit-cycle-configurations
    description: Sandbox API server for testing
tags:
  - name: Internal
    description: Internal endpoints to check the service
  - name: Program transaction types
    description: >-
      Endpoints to associate debit transaction types with a program and a
      transaction category.
  - name: Account accrual rates
    description: Endpoints to set up accrual rates at the account level.
  - name: Program accrual rates
    description: Endpoints to set up accrual rates at the program level.
  - name: Features by account status
    description: >-
      Endpoints to toggle features on or off by account status (at program or
      organization level)
  - name: Additional accrual type configurations
    description: >-
      Endpoints to enable additional accrual type configurations at program
      level
components:
  parameters:
    AccountAccrualTypeQuery:
      name: accrualType
      description: Accrual type
      in: query
      required: false
      schema:
        type: string
        enum:
          - WITHDRAWAL_INTEREST
          - BILLPAYMENT_INTEREST
          - OVERDRAFT_INTEREST
        example: WITHDRAWAL_INTEREST
    AccountIdPath:
      name: accountId
      description: Account ID
      in: path
      required: true
      schema:
        type: integer
        example: 18898
    AccrualTypeConfigurationQuery:
      name: configuration
      description: Optional configuration name to filter by a specific configuration
      in: query
      required: false
      schema:
        type: string
        example: ONCE_AFTER_TRANSACTION_DUE_DATE
    AccrualTypeQuery:
      name: accrualType
      description: Accrual type to filter configurations
      in: query
      required: true
      schema:
        type: string
        enum:
          - WITHDRAWAL_INTEREST
          - BILLPAYMENT_INTEREST
          - OVERDRAFT_INTEREST
          - FINANCIAL_TAX
          - REFINANCING
          - OVERDUE
          - FINE
        example: WITHDRAWAL_INTEREST
    IsDeletedQuery:
      name: isDeleted
      description: Filter by status
      in: query
      schema:
        type: boolean
        default: false
        example: false
    PageQuery:
      name: page
      description: Current page
      in: query
      required: false
      schema:
        type: integer
        default: 1
        example: 1
    PeriodToCalculatePath:
      name: periodToCalculate
      description: Period to calculate the accruals
      in: path
      required: true
      schema:
        type: string
        enum:
          - UNTIL_DUE_DATE
          - AFTER_DUE_DATE
        example: UNTIL_DUE_DATE
    PeriodToCalculateQuery:
      name: periodToCalculate
      description: Period to calculate the accruals
      in: query
      required: false
      schema:
        type: string
        enum:
          - UNTIL_DUE_DATE
          - AFTER_DUE_DATE
        example: UNTIL_DUE_DATE
    PeriodToCalculateQueryRequired:
      name: periodToCalculate
      description: Period to calculate the accruals
      in: query
      required: true
      schema:
        type: string
        enum:
          - UNTIL_DUE_DATE
          - AFTER_DUE_DATE
        example: UNTIL_DUE_DATE
    PerPageQuery:
      name: perPage
      description: Number of items per page
      in: query
      required: false
      schema:
        type: integer
        default: 100
        example: 100
    ProgramIdPath:
      name: programId
      description: Program ID
      in: path
      required: true
      schema:
        type: integer
        example: 5689
    ProgramIdQuery:
      name: programId
      description: Program ID
      in: query
      required: false
      schema:
        type: integer
        format: int64
        example: 12345
    ProgramAccrualTypePath:
      name: accrualType
      description: Accrual type
      in: path
      required: true
      schema:
        type: string
        enum:
          - WITHDRAWAL_INTEREST
          - BILLPAYMENT_INTEREST
          - OVERDRAFT_INTEREST
          - FINANCIAL_TAX
          - REFINANCING
          - OVERDUE
          - FINE
        example: WITHDRAWAL_INTEREST
    ProgramAccrualTypeQuery:
      name: accrualType
      description: Accrual type
      in: query
      required: false
      schema:
        type: string
        enum:
          - WITHDRAWAL_INTEREST
          - BILLPAYMENT_INTEREST
          - OVERDRAFT_INTEREST
          - FINANCIAL_TAX
          - REFINANCING
          - OVERDUE
          - FINE
        example: WITHDRAWAL_INTEREST
    TransactionCategoryIDPath:
      name: transactionCategoryId
      description: Program transaction category ID
      in: path
      required: true
      schema:
        type: integer
        example: 2
    TransactionCategoryIdQuery:
      name: transactionCategoryId
      description: Program transaction category ID
      in: query
      required: false
      schema:
        type: integer
        example: 2
    TransactionCategoryIdQueryRequired:
      name: transactionCategoryId
      description: Transaction category ID to filter configurations
      in: query
      required: true
      schema:
        type: integer
        format: int64
        example: 12345
    ValidityToCalculateQuery:
      name: validityToCalculate
      description: >
        When the rate is used to calculate the accrual. The possible values are:

        - IMMEDIATE — The rate is used to calculate the next generated accruals.

        - DUEDATE — The rate is used to calculate accruals only after the next
        due date.
      in: query
      required: false
      schema:
        type: string
        enum:
          - IMMEDIATE
          - DUEDATE
        example: IMMEDIATE
  schemas:
    AccountAccrualType:
      type: string
      description: |
        Accrual type at the account level. The most commonly used values are:
        - `WITHDRAWAL_INTEREST`—Interest on a withdrawal
        - `BILLPAYMENT_INTEREST`—Interest on a bill payment
        - `OVERDRAFT_INTEREST`—Interest on an overdraft
        - `FINANCIAL_TAX`—Financial tax
      example: WITHDRAWAL_INTEREST
    AccountAccrualTypesRatesRequest:
      type: object
      required:
        - transaction_category_id
        - accrual_type
        - period_to_calculate
        - validity_to_calculate
      properties:
        transaction_category_id:
          $ref: '#/components/schemas/TransactionCategoryID'
        accrual_type:
          $ref: '#/components/schemas/AccountAccrualType'
        period_to_calculate:
          $ref: '#/components/schemas/PeriodToCalculate'
        default_rate:
          $ref: '#/components/schemas/DefaultAccrualRate'
        rate_if_overdue:
          $ref: '#/components/schemas/AccrualRateIfOverdue'
        validity_to_calculate:
          $ref: '#/components/schemas/ValidityToCalculate'
        ranges:
          $ref: '#/components/schemas/AccrualRateRanges'
    AccountAccrualTypesRatesResponse:
      type: object
      required:
        - transaction_category_id
        - accrual_type
        - period_to_calculate
        - validity_to_calculate
      properties:
        transaction_category_id:
          $ref: '#/components/schemas/TransactionCategoryID'
        accrual_type:
          $ref: '#/components/schemas/AccountAccrualType'
        period_to_calculate:
          $ref: '#/components/schemas/PeriodToCalculate'
        default_rate:
          $ref: '#/components/schemas/DefaultAccrualRate'
        rate_if_overdue:
          $ref: '#/components/schemas/AccrualRateIfOverdue'
        ranges:
          $ref: '#/components/schemas/AccrualRateRanges'
        validity_to_calculate:
          $ref: '#/components/schemas/ValidityToCalculate'
        account_accrual_type_rate_id:
          $ref: '#/components/schemas/AccrualTypeRateID'
        account_id:
          $ref: '#/components/schemas/AccountID'
        created_at:
          $ref: '#/components/schemas/CreatedAt'
        deleted_at:
          $ref: '#/components/schemas/DeletedAt'
    AccountID:
      type: integer
      description: Account  ID
      example: 1237456
    AccountStatusItemResponse:
      type: object
      properties:
        name:
          type: string
          description: Account status name
          example: FULL_CANCELLATION
        features:
          type: object
          description: Map of features and the corresponding enabled/disabled status
          properties:
            close-statements:
              type: boolean
            overlimit:
              type: boolean
            late-payment-fee:
              type: boolean
          example:
            close-statements: false
            overlimit: true
      example:
        value:
          - name: DEFINITIVE_CANCELLATION
            features:
              close-statements: false
          - name: BLOCKED
            features:
              close-statements: false
              overlimit: false
    AccountStatusPatchItemRequest:
      type: object
      properties:
        remove_status:
          type: boolean
          description: Remove current status?
          example: true
        name:
          type: string
          description: Account status name
          example: FULL_CANCELLATION
        features:
          type: object
          additionalProperties:
            type: boolean
          description: Map of features and the corresponding enabled/disabled status
          example:
            close-statements: false
            overlimit: true
      example:
        value:
          - name: DEFINITIVE_CANCELLATION
            features:
              close-statements: false
          - remove_status: true
            name: BLOCKED
    AccrualRateIfOverdue:
      type: number
      format: double
      description: >
        Rate used to calculate the accrual when the account is overdue.


        **Notes:**

        - If you don't provide a value for this field, you must provide a value
        for `default_rate`.

        - If you provide a value for `default_rate`, but not `rate_if_overdue`,
        then the Pismo platform only calculates the interest if the account
        <i>is not</i> overdue. If you provide a value for `rate_if_overdue`, but
        not `default_rate`, then the platform only calculates the interest when
        the account <i>is</i> overdue. If you want the platform to calculate the
        interest in both cases, you must provide values for both fields.
      example: 3
    AccrualRateRange:
      required:
        - amount_due_lower_limit
      type: object
      description: Accrual rate ranges
      properties:
        amount_due_lower_limit:
          $ref: '#/components/schemas/AmountDueLowerLimit'
        default_rate:
          $ref: '#/components/schemas/DefaultAccrualRate'
        rate_if_overdue:
          $ref: '#/components/schemas/AccrualRateIfOverdue'
    AccrualRateRanges:
      type: array
      description: >
        List of accrual rate ranges. Which range applies depends on a comparison
        of the last amount due with `amount_due_lower_limit`. For example,
        suppose you execute the endpoint with the following parameter values.

        <pre>

        {
          'transaction_category_id': 107178,
          'accrual_type': 'WITHDRAWAL_INTEREST',
          'period_to_calculate: 'AFTER_DUE_DATE',
          'default_rate': 10.70,
          'rate_if_overdue': 12.70,
          'validity_to_calculate': 'IMMEDIATE',
          'ranges': [
              {
                  'amount_due_lower_limit': 1000,
                  'default_rate': 10.00,
                  'rate_if_overdue': 8.123456
              },
              {
                  'amount_due_lower_limit': 3000,
                  'default_rate': 9.25,
                  'rate_if_overdue': 7.351451
              }
          ]
        }

        </pre>


        If the last amount due is 4000, then both of above ranges are valid, but
        the `amount_due_lower_limit` with 3000 is the closest, so the values for
        `default_rate` and `rate_if_overdue` in the second range are used to
        calculate accruals.


        **Notes:**


        - Either `default_rate`, `rate_if_overdue`, or both must be provided in
        each range.


        - The rate division by total days is not affected by whether or not
        ranges are defined.


        - The **Overdue strategy start** program parameter also impacts the
        amount due. For more information, refer to the entry for that parameter
        in [Program parameters](doc:program-parameters-reference-table).
      items:
        $ref: '#/components/schemas/AccrualRateRange'
    AccrualTypeConfiguration:
      type: string
      description: Accrual type configuration to enable
      enum:
        - ONCE_AFTER_TRANSACTION_DUE_DATE
        - GRACE_DAYS_UNTIL_DUE_DATE_STRATEGY
      example: ONCE_AFTER_TRANSACTION_DUE_DATE
    AccrualTypeConfigurationGetResponse:
      type: object
      required:
        - id
        - transaction_category_id
        - accrual_type
        - period_to_calculate_accrual
        - configuration
        - created_at
      properties:
        id:
          $ref: '#/components/schemas/TransactionCategoryAccrualTypeConfigurationID'
        transaction_category_id:
          $ref: '#/components/schemas/TransactionCategoryID'
        accrual_type:
          $ref: '#/components/schemas/ProgramAccrualType'
        period_to_calculate_accrual:
          $ref: '#/components/schemas/PeriodToCalculateAccrual'
        configuration:
          $ref: '#/components/schemas/AccrualTypeConfiguration'
        value:
          $ref: '#/components/schemas/AccrualTypeConfigurationValue'
        created_at:
          $ref: '#/components/schemas/CreatedAt'
        deleted_at:
          $ref: '#/components/schemas/DeletedAt'
    AccrualTypesConfigurations:
      description: Array of additional accrual type configurations
      type: array
      items:
        type: string
        enum:
          - ONCE_AFTER_TRANSACTION_DUE_DATE
    AccrualTypeConfigurationValue:
      type: integer
      format: int32
      nullable: true
      description: >
        Value for the configuration, if applicable. `null` if the configuration
        does not require a value. Allowed values are 1 or 2.
      example: 1
    AccrualTypeRateID:
      type: integer
      format: int32
      description: Account accrual type rate ID
      example: 1237456
    AmountDueLowerLimit:
      type: number
      format: double
      description: >-
        Lower limit for the range. Must be greater than zero. If the amount due
        is greater than or equal to this amount, `default_rate` and
        `rate_if_overdue` apply.
      example: 4000
    ArrayOfInterestRateObjects:
      type: array
      description: Array of interest rate objects
      items:
        $ref: '#/components/schemas/AccountAccrualTypesRatesResponse'
    ChargeOrder:
      type: integer
      format: int32
      description: >-
        Used to determine the priority order when discharging transaction
        balances. 1 is the highest priority.
      example: 2
    CreatedAt:
      type: string
      description: Date created. Format = yyyy-mm-ddThh:mm:ssZ.
      example: '2023-08-18T20:53:27.498Z'
    CurrentPageNumber:
      type: integer
      format: int32
      description: Current page number
      default: 1
      example: 1
    DefaultAccrualRate:
      type: number
      format: double
      description: >
        Rate used to calculate the accrual when the account is not overdue.


        **Notes:**

        - If you don't provide a value for this field, you must provide a value
        for `rate_if_overdue`.

        - If you provide a value for `default_rate`, but not `rate_if_overdue`,
        then the Pismo platform only calculates the interest if the account
        <i>is not</i> overdue. If you provide a value for `rate_if_overdue`, but
        not `default_rate`, then the platform only calculates the interest when
        the account <i>is</i> overdue. If you want the platform to calculate the
        interest in both cases, you must provide values for both fields.
      example: 2
    DeleteAccrualTypesConfigurations:
      description: Array of additional accrual type configurations for deletion
      type: array
      items:
        type: string
        enum:
          - ONCE_AFTER_TRANSACTION_DUE_DATE
          - GRACE_DAYS_UNTIL_DUE_DATE_STRATEGY
    DeleteAccrualTypeConfigurationsRequest:
      type: object
      properties:
        configurations:
          $ref: '#/components/schemas/DeleteAccrualTypesConfigurations'
      required:
        - configurations
    DeletedAt:
      type: string
      description: Date deleted. Format = yyyy-mm-ddThh:mm:ssZ.
      example: '2023-10-10T10:50:18.212Z'
    FeaturesByAccountStatusPatchRequest:
      type: object
      required:
        - account_statuses
      properties:
        reset_configuration:
          type: boolean
          description: Reset configuration?
          example: true
        program_id:
          type: number
          description: Program ID
          format: int64
          example: 123
        account_statuses:
          type: array
          description: >-
            List of account statuses with the corresponding features marked to
            be disabled/enabled
          items:
            $ref: '#/components/schemas/AccountStatusPatchItemRequest'
    FeaturesByAccountStatusPostRequest:
      type: object
      required:
        - account_statuses
      properties:
        program_id:
          type: number
          description: Program ID
          format: int64
          example: 123
        account_statuses:
          type: array
          description: >-
            List of account statuses with the corresponding features marked to
            be disabled/enabled
          items:
            type: object
            properties:
              name:
                type: string
                description: Account status name
                example: DEFINITIVE_CANCELLATION
              features:
                type: object
                properties:
                  close-statements:
                    type: boolean
                    description: Perform cycle closings?
                    default: true
                    example: false
                  late-payment-fee:
                    type: boolean
                    description: Charge late payment fees?
                    default: true
                    example: false
                  overlimit:
                    type: boolean
                    default: true
                    description: Charge overlimit fees?
                    example: false
                  create-accruals:
                    type: boolean
                    description: |
                      Calculate new accruals?
                    default: true
                    example: false
          example:
            - name: DEFINITIVE_CANCELLATION
              features:
                close-statements: false
    FeaturesByAccountStatusResponse:
      type: object
      required:
        - account_statuses
      properties:
        program_id:
          type: number
          description: Program ID
          format: int64
          example: 123
        account_statuses:
          type: array
          description: >-
            List of account statuses with the corresponding features marked to
            be disabled/enabled
          items:
            $ref: '#/components/schemas/AccountStatusItemResponse'
    GetAllAccountAccrualTypesRatesResponse:
      type: object
      required:
        - current_page
        - per_page
        - total_items
        - items_on_page
        - pages
      properties:
        current_page:
          $ref: '#/components/schemas/CurrentPageNumber'
        per_page:
          $ref: '#/components/schemas/ItemsPerPage'
        total_items:
          $ref: '#/components/schemas/NumberOfItems'
        items_on_page:
          $ref: '#/components/schemas/ItemsOnCurrentPage'
        pages:
          $ref: '#/components/schemas/NumberOfPages'
        is_last_page:
          $ref: '#/components/schemas/IsLastPage'
        items:
          $ref: '#/components/schemas/ArrayOfInterestRateObjects'
    GetAllProgramAccrualTypesRatesResponse:
      type: object
      required:
        - current_page
        - per_page
        - total_items
        - items_on_page
        - pages
      properties:
        current_page:
          $ref: '#/components/schemas/CurrentPageNumber'
        per_page:
          $ref: '#/components/schemas/ItemsPerPage'
        total_items:
          $ref: '#/components/schemas/NumberOfItems'
        items_on_page:
          $ref: '#/components/schemas/ItemsOnCurrentPage'
        pages:
          $ref: '#/components/schemas/NumberOfPages'
        is_last_page:
          $ref: '#/components/schemas/IsLastPage'
        items:
          $ref: '#/components/schemas/ArrayOfInterestRateObjects'
    IsLastPage:
      type: boolean
      description: Indicates if the current page is the last page
      example: false
    ItemsOnCurrentPage:
      type: integer
      format: int32
      description: Number of items on the current page
      example: 10
    ItemsPerPage:
      type: integer
      format: int32
      description: Number of items per page
      minimum: 1
      default: 1
      example: 10
    NumberOfItems:
      type: integer
      format: int32
      description: Number of items
      example: 50
    NumberOfPages:
      type: integer
      format: int32
      description: Number of pages
      example: 5
    PeriodToCalculate:
      type: string
      description: >
        Period to calculate the accrual


        `UNTIL_DUE_DATE`—The period extends from the transaction date plus one
        day to the due date.

        `AFTER_DUE_DATE`—The period extends from the due date plus one day to
        the current date.


        **Note:** If an accrual needs to start at the transaction date and
        continue past the due date, then two accrual rates need to be
        created—one with `UNTIL_DUE_DATE` and another with
        `AFTER_DUE_DATE`.      
    PeriodToCalculateAccrual:
      type: string
      description: Period to calculate the accrual
      enum:
        - UNTIL_DUE_DATE
        - AFTER_DUE_DATE
      example: UNTIL_DUE_DATE
    PostAccrualTypeConfigurationsRequest:
      type: object
      properties:
        configurations:
          $ref: '#/components/schemas/DeleteAccrualTypesConfigurations'
      required:
        - configurations
    PostAccrualTypeConfigurationsResponse:
      type: object
      properties:
        configurations:
          $ref: '#/components/schemas/AccrualTypesConfigurations'
      required:
        - configurations
    PostAccrualTypeConfigurationV2Request:
      type: object
      required:
        - transaction_category_id
        - accrual_type
        - period_to_calculate
        - configuration
      properties:
        transaction_category_id:
          $ref: '#/components/schemas/TransactionCategoryID'
        accrual_type:
          $ref: '#/components/schemas/ProgramAccrualType'
        period_to_calculate:
          $ref: '#/components/schemas/PeriodToCalculate'
        configuration:
          $ref: '#/components/schemas/AccrualTypeConfiguration'
        value:
          $ref: '#/components/schemas/AccrualTypeConfigurationValue'
    PostAccrualTypeConfigurationV2Response:
      type: object
      required:
        - program_id
        - transaction_category_id
        - accrual_type
        - period_to_calculate
        - configuration
        - created_at
      properties:
        program_id:
          type: integer
          format: int64
          description: Program ID
          example: 5689
        transaction_category_id:
          $ref: '#/components/schemas/TransactionCategoryID'
        accrual_type:
          $ref: '#/components/schemas/ProgramAccrualType'
        period_to_calculate:
          $ref: '#/components/schemas/PeriodToCalculate'
        configuration:
          $ref: '#/components/schemas/AccrualTypeConfiguration'
        value:
          $ref: '#/components/schemas/AccrualTypeConfigurationValue'
        created_at:
          $ref: '#/components/schemas/CreatedAt'
    ProgramsTransactionsTypesRequest:
      type: object
      properties:
        transaction_type_id:
          $ref: '#/components/schemas/TransactionTypeID'
        transaction_category_id:
          $ref: '#/components/schemas/TransactionCategoryID'
        charge_order:
          $ref: '#/components/schemas/ChargeOrder'
      required:
        - transaction_type_id
        - transaction_category_id
    ProgramTransactionTypeID:
      type: integer
      description: Program transaction type ID
      example: 156781
    ProgramsTransactionsTypesResponse:
      type: object
      properties:
        program_transaction_type_id:
          $ref: '#/components/schemas/ProgramTransactionTypeID'
        transaction_type_id:
          $ref: '#/components/schemas/TransactionTypeID'
        transaction_category_id:
          $ref: '#/components/schemas/TransactionCategoryID'
        charge_order:
          $ref: '#/components/schemas/ChargeOrder'
      required:
        - program_transaction_type_id
        - transaction_type_id
        - transaction_category_id
    ProgramAccrualType:
      type: string
      description: |
        Accrual type. The possible values at the program level are:
        - `WITHDRAWAL_INTEREST`—Withdrawal interest
        - `BILLPAYMENT_INTEREST`—Bill payment interest
        - `OVERDRAFT_INTEREST`—Overdraft interest
        - `FINANCIAL_TAX`—Financial tax
        - `REFINANCING`—Refinancing charge
        - `OVERDUE`—Overdue charge
        - `FINE`—Overdue fine
      enum:
        - WITHDRAWAL_INTEREST
        - BILLPAYMENT_INTEREST
        - OVERDRAFT_INTEREST
        - FINANCIAL_TAX
        - REFINANCING
        - OVERDUE
        - FINE
      example: WITHDRAWAL_INTEREST
    ProgramAccrualTypesRatesRequest:
      type: object
      required:
        - transaction_category_id
        - accrual_type
        - period_to_calculate
        - validity_to_calculate
      properties:
        transaction_category_id:
          $ref: '#/components/schemas/TransactionCategoryID'
        accrual_type:
          $ref: '#/components/schemas/ProgramAccrualType'
        period_to_calculate:
          $ref: '#/components/schemas/PeriodToCalculate'
        default_rate:
          $ref: '#/components/schemas/DefaultAccrualRate'
        rate_if_overdue:
          $ref: '#/components/schemas/AccrualRateIfOverdue'
        validity_to_calculate:
          $ref: '#/components/schemas/ValidityToCalculate'
        ranges:
          $ref: '#/components/schemas/AccrualRateRanges'
    ProgramAccrualTypesRatesResponse:
      type: object
      required:
        - transaction_category_id
        - accrual_type
        - period_to_calculate
        - validity_to_calculate
        - created_at
      properties:
        transaction_category_id:
          $ref: '#/components/schemas/TransactionCategoryID'
        accrual_type:
          $ref: '#/components/schemas/ProgramAccrualType'
        period_to_calculate:
          $ref: '#/components/schemas/PeriodToCalculate'
        validity_to_calculate:
          $ref: '#/components/schemas/ValidityToCalculate'
        default_rate:
          $ref: '#/components/schemas/DefaultAccrualRate'
        rate_if_overdue:
          $ref: '#/components/schemas/AccrualRateIfOverdue'
        ranges:
          $ref: '#/components/schemas/AccrualRateRanges'
        created_at:
          $ref: '#/components/schemas/CreatedAt'
        deleted_at:
          $ref: '#/components/schemas/DeletedAt'
        accrual_type_rate_id:
          $ref: '#/components/schemas/AccrualTypeRateID'
    StandardErrorResponse:
      type: object
      properties:
        code:
          type: string
          description: Standard error code
          example: '1020030'
        message:
          type: string
          description: Error message
          example: An error occurred when attempting to execute request.
    TransactionCategoryAccrualTypeConfigurationID:
      type: integer
      format: int64
      description: Transaction category accrual type configuration ID
      example: 789
    TransactionCategoryID:
      type: integer
      format: int64
      description: Transaction category ID
      example: 12345
    TransactionTypeID:
      description: Transaction type ID
      type: integer
      example: 101
    ValidityToCalculate:
      type: string
      description: >
        When the rate is used to calculate the accrual. The possible values are:

        - `IMMEDIATE`—The rate is used to calculate the next generated accruals.

        - `DUEDATE`—The rate is used to calculate accruals only after the next
        due date.
      enum:
        - IMMEDIATE
        - DUEDATE
      example: IMMEDIATE
  responses:
    204NoContent:
      description: No Content
    400BadRequest:
      description: Bad Request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/StandardErrorResponse'
          examples:
            Missing request header or param:
              description: Missing request header or parameter
              value:
                code: EX0001
                message: >-
                  Validation Error. Details: Can't find [BalanceAccount] with
                  params: [AccountID: XXXXXX, OrgID:
                  TN-XXXXXXXXXXX-XXXX-XXXX-XXXXXXXXXX].
    404NotFound:
      description: Not Found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/StandardErrorResponse'
          examples:
            Statement not found:
              description: Program ID not found.
              value:
                code: EX0002
                message: Program operationID does not exit.
    409Conflict:
      description: The specified resource status conflicts with the current request.
    500InternalServer:
      description: Internal server error
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
security:
  - BearerAuth: []
paths:
  /v1/programs/{programId}/program-transaction-types:
    post:
      summary: Create program transaction type
      description: >
        A program transaction type is used to associate a transaction type with
        a transaction category. It can also be used to set the charge order. For
        more information, refer to [Using configurable
        entities](doc:full-balance-credit-program#using-configurable-entities).


        **Note:** Only debit transactions and transactions that impact the
        statements can be used to create program transaction types. Use the
        [List transaction types](ref:searchtransactiontypes) endpoint to see if
        a transaction type is a debit (`credit` = `false`) and if it impacts the
        statement (`posted_transaction` = `true`).
      operationId: createProgramTransactionType
      tags:
        - Program transaction types
      parameters:
        - $ref: '#/components/parameters/ProgramIdPath'
      requestBody:
        content:
          application/json:
            schema:
              oneOf:
                - $ref: '#/components/schemas/ProgramsTransactionsTypesRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProgramsTransactionsTypesResponse'
        '400':
          $ref: '#/components/responses/400BadRequest'
        '404':
          $ref: '#/components/responses/404NotFound'
        '500':
          $ref: '#/components/responses/500InternalServer'
  /v1/programs/{programId}/accrual-type-rates:
    post:
      summary: Configure program accrual rates
      description: |
        Configure accrual rates at the program level. 
      operationId: postAccrualTypesRates
      tags:
        - Program accrual rates
      parameters:
        - $ref: '#/components/parameters/ProgramIdPath'
      requestBody:
        content:
          application/json:
            schema:
              oneOf:
                - $ref: '#/components/schemas/ProgramAccrualTypesRatesRequest'
      responses:
        '200':
          description: Ok
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProgramAccrualTypesRatesResponse'
        '400':
          $ref: '#/components/responses/400BadRequest'
        '409':
          $ref: '#/components/responses/409Conflict'
        '500':
          $ref: '#/components/responses/500InternalServer'
    get:
      summary: List program accrual rates
      description: |
        Retrieves a list of accrual rates configured at the program level.
      operationId: listProgramAccrualTypesRates
      tags:
        - Program accrual rates
      parameters:
        - $ref: '#/components/parameters/ProgramIdPath'
        - $ref: '#/components/parameters/PerPageQuery'
        - $ref: '#/components/parameters/PageQuery'
        - $ref: '#/components/parameters/TransactionCategoryIdQuery'
        - $ref: '#/components/parameters/ProgramAccrualTypeQuery'
        - $ref: '#/components/parameters/PeriodToCalculateQuery'
        - $ref: '#/components/parameters/ValidityToCalculateQuery'
        - $ref: '#/components/parameters/IsDeletedQuery'
      responses:
        '200':
          description: Ok
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetAllProgramAccrualTypesRatesResponse'
        '400':
          $ref: '#/components/responses/400BadRequest'
        '500':
          $ref: '#/components/responses/500InternalServer'
    delete:
      summary: Delete program accrual rates
      description: >
        Deletes an accrual rate that's configured for a program.

        If only `programId` is provided, all program accrual rates are deleted
        for that program.

        You can use the optional parameters `transactionCategoryId`,
        `accrualType`, and `periodToCalculate` to specify a subset of the
        program accrual rates that you want to delete.

        For example, if you provide values for `transactionCategoryId`,
        `accrualType`, and `periodToCalculate`, then all program accrual rates
        configured for the specified program, transaction category, accrual
        type, and period to calculate are deleted.
      operationId: deleteAccrualTypesRates
      tags:
        - Program accrual rates
      parameters:
        - $ref: '#/components/parameters/ProgramIdPath'
        - $ref: '#/components/parameters/TransactionCategoryIdQuery'
        - $ref: '#/components/parameters/ProgramAccrualTypeQuery'
        - $ref: '#/components/parameters/PeriodToCalculateQuery'
      responses:
        '204':
          $ref: '#/components/responses/204NoContent'
        '400':
          $ref: '#/components/responses/400BadRequest'
        '404':
          $ref: '#/components/responses/404NotFound'
        '500':
          $ref: '#/components/responses/500InternalServer'
  /v2/programs/{programId}/configuration:
    post:
      summary: Create additional accrual type configuration
      description: >
        Enables an additional configuration on an accrual type for a specific
        transaction category and period to calculate. Any additional
        configuration that you enable modifies the default accrual behavior. 


        **Note:** Pismo recommends you use this endpoint instead of [Enable
        additional configurations on accrual
        types](ref:postaccrualtypesconfigurations), which will eventually be
        deprecated. The new endpoint accepts all parameters in the request body,
        instead of in path parameters.


        ### Available Configurations 


        | Configuration | `value` Required | Allowed `values` | Allowed values
        for `period_to_calculate`

        |---|---|---|---| 

        | `ONCE_AFTER_TRANSACTION_DUE_DATE` | No | N/A | `UNTIL_DUE_DATE`,
        `AFTER_DUE_DATE`

        | `GRACE_DAYS_UNTIL_DUE_DATE_STRATEGY` | Yes | 1 or 2 | `UNTIL_DUE_DATE`


        ### Configuration details 


        - `ONCE_AFTER_TRANSACTION_DUE_DATE`: By default, accruals on
        transactions with this transaction category occur on a daily basis. If
        this additional configuration is enabled, then, for each transaction,
        the accrual is calculated only once on the transaction due date. The
        `value` field is not required for this configuration. 


        - `GRACE_DAYS_UNTIL_DUE_DATE_STRATEGY`: This configuration defines the
        grace days strategy for accruals calculated up to the due date. You must
        set `period_to_calculate` to `UNTIL_DUE_DATE`. The `value` field is
        required. Accepted values are 1 or 2 (meaning Strategy 1 or Strategy 2).
        The strategies are defined as follows.

          - **Strategy 1**: One good cycle restores grace. If the customer misses one cycle, interest is charged for that missed cycle. If the next cycle is fully paid by the due date, grace returns for the following cycle.

          - **Strategy 2**: Two consecutive good cycles are required to restore grace. If the customer misses one cycle, they normally pay interest for the missed cycle and for the next two recovery cycles before grace returns.

        ### `value` behavior 


        - The `value` field accepts an integer (1 or 2). 

        - For configurations that do not require `value` (for example,
        `ONCE_AFTER_TRANSACTION_DUE_DATE`), you can omit the `value` parameter. 

        - For configurations that require `value` (for example,
        `GRACE_DAYS_UNTIL_DUE_DATE_STRATEGY`), you must provide a valid positive
        integer. 

        - If you need to make a new configuration mandatory with a value,
        contact your Pismo representative for help with that—you can't configure
        it using the API. 


        **Note:** Unlike other rates that you define using transaction
        categories or accrual type rates, when you enable the
        `ONCE_AFTER_TRANSACTION_DUE_DATE` configuration on an accrual type, the
        rate you define for that type is *not* divided by the interest rate
        period before it's applied. 
      operationId: post-additional-accrual-type-configuration
      tags:
        - Additional accrual type configurations
      parameters:
        - $ref: '#/components/parameters/ProgramIdPath'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PostAccrualTypeConfigurationV2Request'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PostAccrualTypeConfigurationV2Response'
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
              examples:
                Validation error:
                  description: Request body validation failed
                  value:
                    code: EX0002
                    message: >-
                      Validation Error. Details: transaction_category_id must
                      not be null
                Invalid value for configuration:
                  description: The value provided is not valid for the configuration
                  value:
                    code: EX0004
                    message: >-
                      Validation Error: invalid value for configuration:
                      ONCE_AFTER_TRANSACTION_DUE_DATE
                Value required:
                  description: The configuration requires a value but none was provided
                  value:
                    code: EX0004
                    message: >-
                      Validation Error: value is required and must be a positive
                      integer for configuration: GRACE_PERIOD_IN_DAYS
                Program is not credit type:
                  description: The program is not a credit type program
                  value:
                    code: EX0004
                    message: >-
                      Validation Error: The provided program is not a credit
                      type
        '404':
          description: Not Found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
              examples:
                Program not found:
                  description: The program in the path was not found
                  value:
                    code: EX0004
                    message: >-
                      Validation Error: The program ID XXXXXXXX in the path was
                      not found
                Accrual type configuration not found:
                  description: The configuration name provided does not exist
                  value:
                    code: EX0004
                    message: >-
                      Validation Error: The Accrual type configuration provided
                      (XXXXXXXX) does not exist
                Transaction category not found:
                  description: The transaction category was not found for this program
                  value:
                    code: EX0004
                    message: >-
                      Validation Error: The transaction category XXXXXXXX does
                      not exist or does not belong to the provided program.
                Accrual type not found:
                  description: The accrual type does not exist
                  value:
                    code: EX0004
                    message: >-
                      Validation Error: The accrual type XXXXXXXX does not exist
                      or does not belong to the provided program.
                Period to calculate not found:
                  description: The period to calculate does not exist
                  value:
                    code: EX0004
                    message: >-
                      Validation Error: The periodToCalculate (XXXXXXXX) not
                      found
        '500':
          $ref: '#/components/responses/500InternalServer'
  /v1/programs/{programId}/transaction-category-id/{transactionCategoryId}/accrual-type/{accrualType}/period-to-calculate/{periodToCalculate}/configurations:
    post:
      summary: Enable additional configurations on accrual types
      description: >
        Enable additional configurations on accrual types. Any additional
        configuration that you enable modifies the default accrual behavior.


        Specify configurations to enable by listing them as an array in the
        `configurations` body parameter. The following configuration is
        available.

        - `ONCE_AFTER_TRANSACTION_DUE_DATE`: By default, accruals on
        transactions with this transaction category occur on a daily basis. If
        this additional configuration is enabled, then, for each transaction,
        the accrual is calculated only once on the transaction due date.


        **Note:** Unlike other rates that you define using transaction
        categories or accrual type rates, when you enable the
        `ONCE_AFTER_TRANSACTION_DUE_DATE` configuration on an accrual type, the
        rate you define for that type is <i>not</i> divided by the interest rate
        period before it's applied.
      operationId: postAccrualTypesConfigurations
      tags:
        - Additional accrual type configurations
      parameters:
        - $ref: '#/components/parameters/ProgramIdPath'
        - $ref: '#/components/parameters/TransactionCategoryIDPath'
        - $ref: '#/components/parameters/ProgramAccrualTypePath'
        - $ref: '#/components/parameters/PeriodToCalculatePath'
      requestBody:
        content:
          application/json:
            schema:
              oneOf:
                - $ref: '#/components/schemas/PostAccrualTypeConfigurationsRequest'
      responses:
        '200':
          description: Ok
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PostAccrualTypeConfigurationsResponse'
        '400':
          $ref: '#/components/responses/400BadRequest'
        '404':
          description: Bad Request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
              examples:
                Invalid accrual type configuration passed:
                  description: >-
                    One or more of the configurations in the body is not in the
                    list of the available configurations.
                  value:
                    code: EX0004
                    message: >-
                      Validation Error: The Accrual type configuration provided
                      (MY_INVALID_CONFIGURATION) does not exist
                Program not found:
                  description: >-
                    The program in the path was not found and is not related to
                    the account/tenant.
                  value:
                    code: EX0004
                    message: >-
                      Validation Error: The program ID XXXXXXXX in the path not
                      found
                Transaction category not found:
                  description: >-
                    The transaction category ID in the path was not found and is
                    not related to the account/tenant.
                  value:
                    code: EX0004
                    message: >-
                      Validation Error: The transaction category XXXXXXXX does
                      not exist or does not belong to the provided program.
                Accrual type not found:
                  description: >-
                    The accrual type in the path is not in the list of the
                    available accrual types.
                  value:
                    code: EX0004
                    message: >-
                      Validation Error: The accrual type XXXXXXXX does not exist
                      or does not belong to the provided program.
                Period to calculate not found:
                  description: >-
                    The period to calculate in the path is not in the list of
                    the available periods.
                  value:
                    code: EX0004
                    message: >-
                      Validation Error: The periodToCalculate (XXXXXXXX) not
                      found
        '409':
          $ref: '#/components/responses/409Conflict'
        '500':
          $ref: '#/components/responses/500InternalServer'
    delete:
      summary: Disable additional configuration on accrual types
      description: >
        Disable additional configurations on accrual types. Any additional
        configuration that you disable modifies the default accrual behavior.

        Specify configurations to be disabled by listing them as an array in the
        `configurations` body parameter. The following configurations are
        available.

        - `ONCE_AFTER_TRANSACTION_DUE_DATE`

        - `GRACE_DAYS_UNTIL_DUE_DATE_STRATEGY`<br>


        For information about these strategies, refer to the [Create additional
        accrual type
        configuration](ref:post-additional-accrual-type-configuration) API.
      operationId: deleteAccrualTypesConfigurations
      tags:
        - Additional accrual type configurations
      parameters:
        - $ref: '#/components/parameters/ProgramIdPath'
        - $ref: '#/components/parameters/TransactionCategoryIDPath'
        - $ref: '#/components/parameters/ProgramAccrualTypePath'
        - $ref: '#/components/parameters/PeriodToCalculatePath'
      requestBody:
        content:
          application/json:
            schema:
              oneOf:
                - $ref: '#/components/schemas/DeleteAccrualTypeConfigurationsRequest'
      responses:
        '204':
          description: No Content
        '400':
          $ref: '#/components/responses/400BadRequest'
        '404':
          description: Bad Request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
              examples:
                No configurations to be deleted:
                  description: At least one of the configurations in the body was not found
                  value:
                    code: EX0004
                    message: >-
                      Validation Error: There are no active
                      TransactionsCategoriesAccrualTypesConfigurations with the
                      data provided.
                Invalid accrual type configuration passed:
                  description: >-
                    One or more of the configurations in the body is not in the
                    list of the available configurations.
                  value:
                    code: EX0004
                    message: >-
                      Validation Error: The Accrual type configuration provided
                      (XXXXXXXX) does not exist
                Program not found:
                  description: >-
                    The program in the path was not found and is not related to
                    the account/tenant.
                  value:
                    code: EX0004
                    message: >-
                      Validation Error: The program ID XXXXXXXX in the path was
                      not found
                Transaction category not found:
                  description: >-
                    The transaction category in the path was not found and is
                    not related to the account/tenant.
                  value:
                    code: EX0004
                    message: >-
                      Validation Error: The transaction category XXXXXXXX does
                      not exist or does not belong to the provided program.
                Accrual type not found:
                  description: >-
                    The accrual type in the path is not in the list of the
                    available accrual types.
                  value:
                    code: EX0004
                    message: >-
                      Validation Error: The accrual type XXXXXXXX does not exist
                      or does not belong to the provided program.
                Period to calculate not found:
                  description: >-
                    The period to calculate in the path is not in the list of
                    the available periods.
                  value:
                    code: EX0004
                    message: >-
                      Validation Error: The periodToCalculate (XXXXXXXX) not
                      found
        '409':
          $ref: '#/components/responses/409Conflict'
        '500':
          $ref: '#/components/responses/500InternalServer'
  /v1/programs/{programId}/configurations:
    get:
      summary: List additional accrual type configurations
      description: >
        Retrieves a list of additional accrual type configurations for a
        program, filtered by transaction category, accrual type, and period to
        calculate.


        Optionally, you can filter by a specific configuration name to retrieve
        only that configuration.


        The response includes both active and deleted configurations. Deleted
        configurations have a non-null `deleted_at` field.
      operationId: findConfigurationByFilter
      tags:
        - Additional accrual type configurations
      parameters:
        - $ref: '#/components/parameters/ProgramIdPath'
        - $ref: '#/components/parameters/TransactionCategoryIdQueryRequired'
        - $ref: '#/components/parameters/AccrualTypeQuery'
        - $ref: '#/components/parameters/PeriodToCalculateQueryRequired'
        - $ref: '#/components/parameters/AccrualTypeConfigurationQuery'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AccrualTypeConfigurationGetResponse'
        '400':
          $ref: '#/components/responses/400BadRequest'
        '500':
          $ref: '#/components/responses/500InternalServer'
  /v1/accounts/{accountId}/accrual-type-rates:
    post:
      summary: Configure account accrual rates
      description: >
        Configure accrual rates for individual accounts.

        To do this, you must first configure the rates at the program level
        using the <a
        href="https://developers.pismo.io/pismo-docs/reference/postaccrualtypesrates"
        target="_blank">Configure program accrual rates</a> endpoint.

        Then, use this endpoint to override the configuration for an individual
        account.

        Use the `accrual_type` field to specify the type of accrual.

        The `periodToCalculate` must be set to `UNTIL_DUE_DATE`, which means the
        accrual stops accruing when the due date is reached.


        NOTE: This endpoint requires an account-specific access token. Getting
        an account token requires you to call [Get OpenID access
        token](ref:post-passport-v1-oauth2-token-1) with an external account ID.
        Tokens can expire quickly, which can result in an Unauthorized message.
      operationId: postAccountAccrualTypesRates
      tags:
        - Account accrual rates
      parameters:
        - $ref: '#/components/parameters/AccountIdPath'
      requestBody:
        content:
          application/json:
            schema:
              oneOf:
                - $ref: '#/components/schemas/AccountAccrualTypesRatesRequest'
      responses:
        '200':
          description: Ok
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountAccrualTypesRatesResponse'
        '400':
          $ref: '#/components/responses/400BadRequest'
        '409':
          $ref: '#/components/responses/409Conflict'
        '500':
          $ref: '#/components/responses/500InternalServer'
    get:
      summary: List account accrual rates
      description: >
        Retrieves a list of accrual rates configured at the account level.


        NOTE: This endpoint requires an account-specific access token. Getting
        an account token requires you to call [Get OpenID access
        token](ref:post-passport-v1-oauth2-token-1) with an external account ID.
        Tokens can expire quickly, which can result in an Unauthorized message.
      operationId: listAccountAccrualTypeRates
      tags:
        - Account accrual rates
      parameters:
        - $ref: '#/components/parameters/AccountIdPath'
        - $ref: '#/components/parameters/PerPageQuery'
        - $ref: '#/components/parameters/PageQuery'
        - $ref: '#/components/parameters/TransactionCategoryIdQuery'
        - $ref: '#/components/parameters/AccountAccrualTypeQuery'
        - $ref: '#/components/parameters/PeriodToCalculateQuery'
        - $ref: '#/components/parameters/ValidityToCalculateQuery'
      responses:
        '200':
          description: Ok
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetAllAccountAccrualTypesRatesResponse'
        '400':
          $ref: '#/components/responses/400BadRequest'
        '500':
          $ref: '#/components/responses/500InternalServer'
    delete:
      summary: Delete account accrual rates
      description: >
        Deletes an accrual rate that's configured for an individual account.

        If only `accountId` is provided, all account accrual rates are deleted
        for that account.

        You can use the optional parameters `transactionCategoryId`,
        `accrualType`, and `periodToCalculate` to specify a subset of the
        account accrual rates that you want to delete.

        For example, if you provide values for `transactionCategoryId`,
        `accrualType`, and `periodToCalculate`, then all account accrual rates
        configured for the specified account, transaction category, accrual
        type, and period to calculate are deleted.


        NOTE: This endpoint requires an account-specific access token. Getting
        an account token requires you to call [Get OpenID access
        token](ref:post-passport-v1-oauth2-token-1) with an external account ID.
        Tokens can expire quickly, which can result in an Unauthorized message.
      operationId: deleteAccountAccrualTypesRate
      tags:
        - Account accrual rates
      parameters:
        - $ref: '#/components/parameters/AccountIdPath'
        - $ref: '#/components/parameters/TransactionCategoryIdQuery'
        - $ref: '#/components/parameters/AccountAccrualTypeQuery'
        - $ref: '#/components/parameters/PeriodToCalculateQuery'
      responses:
        '204':
          $ref: '#/components/responses/204NoContent'
        '400':
          $ref: '#/components/responses/400BadRequest'
        '404':
          $ref: '#/components/responses/404NotFound'
        '500':
          $ref: '#/components/responses/500InternalServer'
  /v1/features-by-account-status:
    post:
      summary: Configure features by account status
      description: >
        Configure features by account status at the program or organization
        level.

        <b>Note:</b> This only applies to full balance credit card programs.


        You can toggle the following features on or off for a particular account
        status by setting them to `true` or `false`, respectively. <b>By
        default, all these features are on (`true`)</b>.

        * `close-statements`: If `false`, the platform doesn't perform any cycle
        closings if the account has the specified status.

        * `late-payment-fee`: If `false`, the platform doesn't charge any late
        payment fees if the account has the specified status.

        * `overlimit`: If `false`, the platform doesn't charge any overlimit
        fees if the account has the specified status.

        * `create-accruals`: If `false`, the platform doesn't calculate new
        accruals if the account has the specified status.


        For example, suppose you set the `close-statements` feature to `false`
        for the `CANCELLED` account status for a specific program. (That is, you
        turn the feature off at the program level.) Then the Pismo platform
        won't perform any cycle closings for accounts in that status if they
        belong to that program.


        <b>Important:</b> Once the status is set to bypass the billing cycle
        closure and the account reaches that status, it cannot revert to a
        previous status. In other words, no further cycle closures can occur for
        this account. The change is irreversible.


        There are several points you should keep in mind when using this
        endpoint.

        * You can use this endpoint to set a feature toggle at the organization
        level, whether or not you have created any full balance credit programs
        in your organization. However, if you try to use it to set a feature
        toggle at the program level, and the program isn't a full balance credit
        card program, the endpoint throws an error.

        * If a feature is set to `true` or `false` at the program level, this
        overrules that feature's setting at the organization level. For example,
        if `close-statements` is set to `false` at the organization level and
        `true` at the program level, then the platform continues to perform
        cycle closings for accounts in the specified program. In other words,
        the organization level setting is ignored.

        * If a feature is not set to `true` or `false` at either the program or
        organization level, it defaults to `true`.

        * You can't use this endpoint to update a feature setting for an account
        status if it has already been configured at a particular level. For
        that, you need to use [Update features by account
        status](ref:patchfeaturesbyaccountstatus).


        You can use the [List statuses](ref:v4-get-account-status) endpoint to
        list all the account statuses defined for your organization.
      operationId: postFeaturesByAccountStatus
      tags:
        - Features by account status
      requestBody:
        content:
          application/json:
            schema:
              oneOf:
                - $ref: '#/components/schemas/FeaturesByAccountStatusPostRequest'
      responses:
        '201':
          description: Created
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
              examples:
                Missing request header or param:
                  description: x-tenant
                  value:
                    code: EX0001
                    message: 'Request header is missing: x-tenant'
                Status not configured:
                  description: FULL_CANCELLATION
                  value:
                    code: WCCC011
                    message: >-
                      Validation Error: The "FULL_CANCELLATION" account status
                      was not found for this organization.
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
              examples:
                Internal Server Error:
                  value:
                    code: GN0001
                    message: Generic Error
    get:
      summary: List features by account status
      description: >
        List account statuses with the corresponding feature toggles at program
        or organization level.
      operationId: getFeaturesByAccountStatus
      tags:
        - Features by account status
      parameters:
        - $ref: '#/components/parameters/ProgramIdQuery'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeaturesByAccountStatusResponse'
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
              examples:
                Missing request header or param:
                  description: x-tenant
                  value:
                    code: EX0001
                    message: 'Request header is missing: x-tenant'
        '404':
          description: Not Found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
              examples:
                Configuration not found:
                  description: x-tenant
                  value:
                    code: EX0004
                    message: >-
                      Validation Error: Configuration not found for this
                      organization.
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
              examples:
                Internal Server Error:
                  value:
                    code: GN0001
                    message: Generic Error
    patch:
      summary: Update features by account status
      description: >
        Update features by account status at the program or organization level.

        <b>Note:</b> This only applies to full balance credit card programs, and
        only features that were previously configured using [Configure features
        by account status](ref:postfeaturesbyaccountstatus) can be updated using
        this endpoint.


        You can toggle the following features on or off for a particular account
        status by setting them to `true` or `false`, respectively. <b>By
        default, all these features are on (`true`)</b>. In the `FEATURES
        OBJECT` body parameter, `newKey`, `newKey-1`, and so on, must be one of
        these features.

        * `close-statements`: If `false`, the platform won't perform any cycle
        closings if the account has the specified status.

        * `late-payment-fee`: If `false`, the platform won't charge any late
        payment fees if the account has the specified status.

        * `overlimit`: If `false`, the platform won't charge any overlimit fees
        if the account has the specified status.


        When modifying an existing account status, all the features previously
        configured must be provided.

        <b>Note:</b> If you send a feature name that does not exist, the
        endpoint returns an error.


        A status can be removed if the `remove_status` flag is set to `true`.  


        The configuration can be reset if the `reset_configuration` flag is set
        to `true`. Resetting the configuration causes all the features to go
        back to their default values (`true`).

        <b>Note:</b> if you set `reset_configuration` to true, and also try to
        update some features for that status, the endpoint generates the
        following error:

        <b>Request body is missing: account_statuses field is not allowed when
        it is set to reset this configuration.</b>


        You can use the [List statuses](ref:v4-get-account-status) endpoint to
        list all the account statuses defined for your organization.           
      operationId: patchFeaturesByAccountStatus
      tags:
        - Features by account status
      requestBody:
        content:
          application/json:
            schema:
              oneOf:
                - $ref: '#/components/schemas/FeaturesByAccountStatusPatchRequest'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeaturesByAccountStatusResponse'
        '400':
          description: Bad Request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
              examples:
                Missing request header or param:
                  description: x-tenant
                  value:
                    code: EX0001
                    message: 'Request header is missing: x-tenant'
                Status not configured:
                  description: FULL_CANCELLATION
                  value:
                    code: WCCC011
                    message: >-
                      Validation Error: The "FULL_CANCELLATION" account status
                      was not found for this organization.
        '404':
          description: Not Found
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
              examples:
                Internal Server Error:
                  value:
                    code: GN0001
                    message: Generic Error
